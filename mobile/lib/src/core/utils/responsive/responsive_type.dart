/// Device types for responsive design
enum DeviceType { mobile, tablet, desktop, tv, watch }

/// Screen sizes for responsive design
enum ScreenSize { small, medium, large, extraLarge }

/// Platform types
enum PlatformType { ios, android, macos, windows, linux, web, fuchsia }
