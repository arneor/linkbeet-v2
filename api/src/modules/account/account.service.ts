import { Injectable } from '@nestjs/common'

/**
 * AccountService — mode field management (normal|business),
 * one-time Business mode activation, industry tag CRUD,
 * default_landing_screen preference.
 */
@Injectable()
export class AccountService {
  constructor() {}
}
