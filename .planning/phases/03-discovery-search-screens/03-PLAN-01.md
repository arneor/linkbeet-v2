---
phase: 3
plan: 01
type: scaffold
wave: 1
depends_on: []
files_modified:
  - mobile/ (entire directory — replaced from Expo to Flutter)
  - mobile/pubspec.yaml
  - mobile/analysis_options.yaml
  - mobile/lib/main.dart
  - mobile/lib/src/core/**
  - mobile/lib/src/modules/**
autonomous: true
requirements:
  - DISC-01
  - DISC-02
  - MOB-07
  - MOB-08
---

<objective>
Replace the existing Expo React Native mobile app with a Flutter project using Clean Architecture.
Remove all Expo/React Native files, create a new Flutter project, and scaffold the complete
Clean Architecture folder structure as specified by the user. Configure pubspec.yaml with core
dependencies (flutter_bloc, go_router, get_it, freezed, dio, google_fonts, equatable).
</objective>

<tasks>

## Task 1: Remove Expo React Native Files

<type>shell</type>

<read_first>

- mobile/package.json (understand current structure before removal)
- mobile/app.json (Expo config)
- mobile/.gitignore </read_first>

<files>
- mobile/ (everything except mobile/.gitignore — will be recreated)
</files>

<action>
Remove all Expo React Native files from `mobile/` directory:

```bash
cd mobile
# Remove Expo-specific directories
rm -rf node_modules .expo .turbo __tests__ skills docs app src assets
# Remove Expo-specific files
rm -f package.json app.json babel.config.js metro.config.js tsconfig.json index.ts
rm -f .prettierrc.js eslint.config.mjs eas.json
rm -f .env.development .env.example .env.production .env.staging
```

Keep `.gitignore` — will be replaced with Flutter version in next task. </action>

<acceptance_criteria>

- `mobile/package.json` does NOT exist
- `mobile/app.json` does NOT exist
- `mobile/node_modules/` does NOT exist
- `mobile/src/` does NOT exist (React Native src)
- `mobile/.expo/` does NOT exist
- `mobile/metro.config.js` does NOT exist </acceptance_criteria>

---

## Task 2: Create Flutter Project

<type>shell</type>

<read_first>

- (none — fresh creation) </read_first>

<files>
- mobile/ (Flutter project root)
</files>

<action>
Create a new Flutter project in the `mobile/` directory:

```bash
cd /Users/nidhin/My/DEVELOPMENT/BUSSINESS/WEB/linkbeet-v2
flutter create --org com.linkbeet --project-name linkbeet_mobile --platforms ios,android mobile
```

This creates the standard Flutter project scaffold with:

- `lib/main.dart` — entry point
- `pubspec.yaml` — dependency config
- `analysis_options.yaml` — linting
- `android/` — Android platform files
- `ios/` — iOS platform files
- `test/` — test directory

After creation, verify the project compiles:

```bash
cd mobile && flutter pub get
```

</action>

<acceptance_criteria>

- `mobile/pubspec.yaml` exists and contains `name: linkbeet_mobile`
- `mobile/lib/main.dart` exists
- `mobile/android/` directory exists
- `mobile/ios/` directory exists
- `mobile/test/` directory exists
- `flutter pub get` in `mobile/` exits with code 0 </acceptance_criteria>

---

## Task 3: Configure pubspec.yaml with Core Dependencies

<type>code</type>

<read_first>

- mobile/pubspec.yaml (the auto-generated one from flutter create) </read_first>

<files>
- mobile/pubspec.yaml
</files>

<action>
Replace the contents of `mobile/pubspec.yaml` with:

```yaml
name: linkbeet_mobile
description: LinkBeet — Hyperlocal Community Commerce Platform
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter

  # State Management
  flutter_bloc: ^9.1.0
  equatable: ^2.0.7

  # Routing
  go_router: ^15.1.0

  # Dependency Injection
  get_it: ^8.0.3
  injectable: ^2.5.0

  # Networking
  dio: ^5.8.0

  # Data Models & Serialization
  freezed_annotation: ^3.0.0
  json_annotation: ^4.9.0

  # Functional Programming
  dartz: ^0.10.1

  # UI
  google_fonts: ^6.2.1
  flutter_svg: ^2.0.17
  cached_network_image: ^3.4.1

  # Utils
  intl: ^0.20.2
  shared_preferences: ^2.5.3
  flutter_secure_storage: ^9.2.4
  logger: ^2.5.0
  url_launcher: ^6.3.1

  # Platform
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.4.14
  freezed: ^3.0.0
  json_serializable: ^6.9.4
  injectable_generator: ^2.7.0
  bloc_test: ^9.1.7
  mocktail: ^1.0.4
```

Then run:

```bash
cd mobile && flutter pub get
```

</action>

<acceptance_criteria>

- `mobile/pubspec.yaml` contains `flutter_bloc: ^9.1.0`
- `mobile/pubspec.yaml` contains `go_router: ^15.1.0`
- `mobile/pubspec.yaml` contains `get_it: ^8.0.3`
- `mobile/pubspec.yaml` contains `freezed_annotation: ^3.0.0`
- `mobile/pubspec.yaml` contains `dio: ^5.8.0`
- `mobile/pubspec.yaml` contains `google_fonts: ^6.2.1`
- `flutter pub get` in `mobile/` exits with code 0 </acceptance_criteria>

---

## Task 4: Configure analysis_options.yaml

<type>code</type>

<read_first>

- mobile/analysis_options.yaml (auto-generated) </read_first>

<files>
- mobile/analysis_options.yaml
</files>

<action>
Replace `mobile/analysis_options.yaml` with:

```yaml
include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - '**/*.g.dart'
    - '**/*.freezed.dart'
    - '**/*.config.dart'
    - 'lib/generated/**'
  errors:
    invalid_annotation_target: ignore

linter:
  rules:
    - always_declare_return_types
    - annotate_overrides
    - avoid_empty_else
    - avoid_print
    - avoid_relative_lib_imports
    - avoid_returning_null_for_future
    - avoid_slow_async_io
    - avoid_types_as_parameter_names
    - avoid_unnecessary_containers
    - avoid_web_libraries_in_flutter
    - cancel_subscriptions
    - close_sinks
    - constant_identifier_names
    - directives_ordering
    - no_duplicate_case_values
    - no_logic_in_create_state
    - prefer_const_constructors
    - prefer_const_constructors_in_immutables
    - prefer_const_declarations
    - prefer_const_literals_to_create_immutables
    - prefer_final_fields
    - prefer_final_in_for_each
    - prefer_final_locals
    - prefer_if_null_operators
    - prefer_single_quotes
    - require_trailing_commas
    - sized_box_for_whitespace
    - sort_child_properties_last
    - unnecessary_await_in_return
    - unnecessary_const
    - unnecessary_new
    - unnecessary_null_in_if_null_operators
    - unnecessary_string_interpolations
    - use_build_context_synchronously
    - use_key_in_widget_constructors
```

</action>

<acceptance_criteria>

- `mobile/analysis_options.yaml` contains `include: package:flutter_lints/flutter.yaml`
- `mobile/analysis_options.yaml` contains `avoid_relative_lib_imports`
- `mobile/analysis_options.yaml` contains `prefer_const_constructors`
- `mobile/analysis_options.yaml` contains `require_trailing_commas` </acceptance_criteria>

---

## Task 5: Scaffold Clean Architecture Folder Structure

<type>shell</type>

<read_first>

- (none — creating fresh directories) </read_first>

<files>
- mobile/lib/src/core/**
- mobile/lib/src/modules/**
</files>

<action>
Create the complete Clean Architecture folder structure. Run from `mobile/`:

```bash
# Core directory structure
mkdir -p lib/src/core/config
mkdir -p lib/src/core/services/network
mkdir -p lib/src/core/services/storage
mkdir -p lib/src/core/ui/theme
mkdir -p lib/src/core/ui/widgets
mkdir -p lib/src/core/ui/assets
mkdir -p lib/src/core/utils/constants
mkdir -p lib/src/core/utils/extensions
mkdir -p lib/src/core/utils/formatters
mkdir -p lib/src/core/utils/validators
mkdir -p lib/src/core/domain/usecases
mkdir -p lib/src/core/domain/errors
mkdir -p lib/src/core/di
mkdir -p lib/src/core/router

# Foundation module (app initialization)
mkdir -p lib/src/modules/foundation/app/presentation/ui/pages

# Discovery module (Phase 3 — Discovery & Search)
mkdir -p lib/src/modules/discovery/data/datasources
mkdir -p lib/src/modules/discovery/data/models
mkdir -p lib/src/modules/discovery/data/repositories
mkdir -p lib/src/modules/discovery/domain/entities
mkdir -p lib/src/modules/discovery/domain/repositories
mkdir -p lib/src/modules/discovery/domain/usecases
mkdir -p lib/src/modules/discovery/presentation/logic
mkdir -p lib/src/modules/discovery/presentation/ui/pages
mkdir -p lib/src/modules/discovery/presentation/ui/widgets
mkdir -p lib/src/modules/discovery/presentation/router

# Add .gitkeep to all empty directories
find lib/src -type d -empty -exec touch {}/.gitkeep \;
```

The resulting structure matches the user's Clean Architecture specification:

```
lib/src/
├── core/
│   ├── config/
│   ├── services/
│   │   ├── network/
│   │   └── storage/
│   ├── ui/
│   │   ├── theme/
│   │   ├── widgets/
│   │   └── assets/
│   ├── utils/
│   │   ├── constants/
│   │   ├── extensions/
│   │   ├── formatters/
│   │   └── validators/
│   ├── domain/
│   │   ├── usecases/
│   │   └── errors/
│   ├── di/
│   └── router/
└── modules/
    ├── foundation/
    │   └── app/
    │       └── presentation/ui/pages/
    └── discovery/
        ├── data/
        │   ├── datasources/
        │   ├── models/
        │   └── repositories/
        ├── domain/
        │   ├── entities/
        │   ├── repositories/
        │   └── usecases/
        └── presentation/
            ├── logic/
            ├── ui/
            │   ├── pages/
            │   └── widgets/
            └── router/
```

</action>

<acceptance_criteria>

- `mobile/lib/src/core/config/` directory exists
- `mobile/lib/src/core/services/network/` directory exists
- `mobile/lib/src/core/ui/theme/` directory exists
- `mobile/lib/src/core/ui/widgets/` directory exists
- `mobile/lib/src/core/domain/usecases/` directory exists
- `mobile/lib/src/core/domain/errors/` directory exists
- `mobile/lib/src/core/di/` directory exists
- `mobile/lib/src/core/router/` directory exists
- `mobile/lib/src/modules/foundation/app/presentation/ui/pages/` directory exists
- `mobile/lib/src/modules/discovery/data/datasources/` directory exists
- `mobile/lib/src/modules/discovery/data/models/` directory exists
- `mobile/lib/src/modules/discovery/data/repositories/` directory exists
- `mobile/lib/src/modules/discovery/domain/entities/` directory exists
- `mobile/lib/src/modules/discovery/domain/repositories/` directory exists
- `mobile/lib/src/modules/discovery/domain/usecases/` directory exists
- `mobile/lib/src/modules/discovery/presentation/logic/` directory exists
- `mobile/lib/src/modules/discovery/presentation/ui/pages/` directory exists
- `mobile/lib/src/modules/discovery/presentation/ui/widgets/` directory exists
- `mobile/lib/src/modules/discovery/presentation/router/` directory exists </acceptance_criteria>

---

## Task 6: Create .gitignore for Flutter

<type>code</type>

<read_first>

- (none) </read_first>

<files>
- mobile/.gitignore
</files>

<action>
Replace `mobile/.gitignore` with comprehensive Flutter gitignore:

```gitignore
# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.buildlog/
.history
.svn/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# VS Code related
.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
/build/
pubspec.lock

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Generated files
*.g.dart
*.freezed.dart
*.config.dart
lib/generated/

# Environment
.env
.env.*
!.env.example

# Coverage
coverage/
```

</action>

<acceptance_criteria>

- `mobile/.gitignore` contains `.dart_tool/`
- `mobile/.gitignore` contains `*.g.dart`
- `mobile/.gitignore` contains `*.freezed.dart`
- `mobile/.gitignore` contains `/build/` </acceptance_criteria>

</tasks>

<verification>
1. Run `cd mobile && flutter pub get` — should exit 0
2. Run `cd mobile && flutter analyze` — should pass (allow info-level only)
3. Verify folder structure: `find mobile/lib/src -type d | sort` should show all Clean Architecture directories
4. Verify no Expo remnants: `ls mobile/package.json` should fail (file not found)
5. Verify no React Native remnants: `find mobile -name "*.tsx" -o -name "*.jsx"` should return empty
</verification>

<success_criteria>

- [ ] All Expo React Native files removed from `mobile/`
- [ ] Flutter project created with `flutter create`
- [ ] `flutter pub get` succeeds
- [ ] Core dependencies configured in pubspec.yaml (flutter_bloc, go_router, get_it, freezed, dio,
      google_fonts)
- [ ] Clean Architecture folder structure fully scaffolded (core/, modules/foundation/,
      modules/discovery/)
- [ ] analysis_options.yaml configured with strict linting
- [ ] .gitignore updated for Flutter </success_criteria>

<must_haves>

- Flutter project compiles (`flutter pub get` succeeds)
- Clean Architecture folder structure matches user-specified pattern exactly
- No Expo/React Native remnants in mobile/ </must_haves>
