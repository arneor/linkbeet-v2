---
phase: 3
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - mobile/lib/src/modules/home/presentation/ui/screens/home_screen.dart
  - mobile/lib/src/modules/home/presentation/ui/widgets/discovery_search_input.dart
  - mobile/lib/src/modules/home/presentation/ui/widgets/filter_chip_row.dart
  - mobile/lib/src/modules/home/presentation/ui/widgets/suggestion_pill_row.dart
autonomous: true
requirements:
  - DISC-01
  - DISC-02
  - DISC-03
  - DISC-04
  - MOB-07

must_haves:
  truths:
    - "User sees a 'LinkBeet' logo + wordmark centered at the top of the discovery screen"
    - 'User sees a single-line search pill (h=52px, rounded-full, white bg, slate-200 border) with a
      Search icon on the left and mic+send buttons on the right'
    - 'Search pill border animates to accentFocusBorder (rgba(0,113,227,0.40)) on focus'
    - 'Send button turns Apple Blue (#0071e3) when query text is non-empty'
    - 'A horizontal scrollable row of filter chips (Filters, Restaurants, Salons & Spas, Creators,
      Open Now) appears below the search pill'
    - 'Three suggestion pills appear below filter chips in a horizontal scrollable row'
    - 'On mobile, the search input is positioned floating at the bottom of the screen via
      Stack+Positioned(bottom:16)'
    - 'The logo, filter chips, and suggestion pills enter with staggered fadeInUp animations using
      AppDurations constants'
    - 'No inline hex color values or hardcoded font sizes appear in any of the new files'
    - 'Trending searches list appears when search pill is focused (on mobile: always visible below
      pill)'
  artifacts:
    - path: 'mobile/lib/src/modules/home/presentation/ui/screens/home_screen.dart'
      provides: "Discovery home screen — full implementation replacing Text('Home') stub"
      min_lines: 120
    - path: 'mobile/lib/src/modules/home/presentation/ui/widgets/discovery_search_input.dart'
      provides: 'Reusable search pill widget with focus animation, compact mode for search results'
      exports: ['DiscoverySearchInput']
    - path: 'mobile/lib/src/modules/home/presentation/ui/widgets/filter_chip_row.dart'
      provides: 'Horizontal scrollable filter chips row'
      exports: ['FilterChipRow']
    - path: 'mobile/lib/src/modules/home/presentation/ui/widgets/suggestion_pill_row.dart'
      provides: 'Horizontal scrollable suggestion pills row'
      exports: ['SuggestionPillRow']
  key_links:
    - from: 'home_screen.dart'
      to: 'discovery_search_input.dart'
      via: 'import + instantiation in Positioned(bottom: 16)'
      pattern: 'DiscoverySearchInput'
    - from: 'home_screen.dart'
      to: 'filter_chip_row.dart'
      via: 'import + instantiation in scrollable column'
      pattern: 'FilterChipRow'
    - from: 'home_screen.dart'
      to: 'suggestion_pill_row.dart'
      via: 'import + instantiation in scrollable column'
      pattern: 'SuggestionPillRow'
    - from: 'discovery_search_input.dart'
      to: 'AppColors.accentFocusBorder'
      via: 'AnimatedContainer border color toggle on _isFocused'
      pattern: 'accentFocusBorder'
---

<objective>
Implement the Flutter Discovery Home screen that replicates `web/src/app/page.tsx` exactly for mobile.

Replace the stub `HomeScreen` (currently `Text('Home')`) with the full Discovery screen: logo +
wordmark, search pill with trending dropdown, filter chips row, and suggestion pills row. The search
input floats at the bottom (M-01 pattern). All three widget files are created as standalone
composable widgets consumed by the screen.

Purpose: This is the primary user-facing screen — the first thing users see when the app launches.
Must match the "Spatial AI Light" design exactly. Output: 4 Dart files — 1 screen + 3 widget files.
</objective>

<execution_context> @/Users/nidhin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nidhin/.claude/get-shit-done/templates/summary.md </execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-discovery-search-screens/03-CONTEXT.md
@.planning/phases/03-discovery-search-screens/03-PATTERNS.md

<!-- Web reference implementation — the exact design to replicate -->

@web/src/app/page.tsx

<!-- Existing theme constants — use these exclusively, no inline values -->

@mobile/lib/src/core/theme/app_colors.dart @mobile/lib/src/core/theme/app_font_sizes.dart
@mobile/lib/src/core/theme/app_radius.dart @mobile/lib/src/core/theme/app_spacing.dart
@mobile/lib/src/core/theme/app_sizes.dart @mobile/lib/src/core/theme/app_durations.dart
@mobile/lib/src/core/theme/app_text_style.dart

<!-- Existing screens for pattern reference -->

@mobile/lib/src/modules/foundation/app/presentation/ui/screens/splash_screen.dart
@mobile/lib/src/modules/home/presentation/router/home_router_module.dart

<interfaces>
<!-- Key constants available in theme files — executor must use these, not inline values -->

From mobile/lib/src/core/theme/app_colors.dart: AppColors.pageBg = Color(0xFFFFFFFF)
AppColors.secondaryBg = Color(0xFFF5F5F7) AppColors.inputBg = Color(0xFFFFFFFF)
AppColors.textPrimary = Color(0xFF1D1D1F) AppColors.textSecondary = Color(0xFF475569)
AppColors.textTertiary = Color(0xFF64748B) AppColors.textPlaceholder = Color(0xFFCBD5E1)
AppColors.accent = Color(0xFF0071E3) AppColors.accentTintBg = Color(0x1A0071E3) // 10% opacity blue
AppColors.accentFocusBorder = Color(0x660071E3) // 40% opacity blue AppColors.border =
Color(0xFFE2E8F0) // slate-200 AppColors.success = Color(0xFF10B981) // emerald-500 (suggestion pill
icon) AppColors.openNowText = Color(0xFF065F46) // emerald-700 AppColors.openNowBg =
Color(0xFFD1FAE5) // emerald-50 AppColors.openNowBorder = Color(0xFF6EE7B7) // emerald-200
AppColors.cardShadow = [BoxShadow(color: Color(0x0F000000), blurRadius: 30, offset: Offset(0, 8))]
AppColors.focusShadow = [BoxShadow(color: Color(0x260071E3), blurRadius: 30, offset: Offset(0, 4))]

From mobile/lib/src/core/theme/app_font_sizes.dart: AppFontSizes.heroGreeting = 30.0
AppFontSizes.body = 17.0 AppFontSizes.filterChip = 13.0 AppFontSizes.suggestionPill = 14.0
AppFontSizes.badge = 11.0 AppFontSizes.inputText = 17.0

From mobile/lib/src/core/theme/app_radius.dart: AppRadius.searchInput = BorderRadius.circular(24) //
main search pill AppRadius.large = BorderRadius.circular(12) // suggestion pills, compact search
AppRadius.pill = BorderRadius.circular(9999) // filter chips AppRadius.sendButton =
BorderRadius.circular(20) // send button

From mobile/lib/src/core/theme/app_sizes.dart: AppSizes.sendButtonSize = 40.0 AppSizes.iconSm = 16.0
AppSizes.iconLg = 24.0 AppSizes.touchTarget = 44.0

From mobile/lib/src/core/theme/app_spacing.dart: AppSpacing.screenPadding =
EdgeInsets.symmetric(horizontal: 16) AppSpacing.filterChipPadding = EdgeInsets.symmetric(horizontal:
12, vertical: 6) AppSpacing.suggestionPillPadding = EdgeInsets.symmetric(horizontal: 16,
vertical: 10) AppSpacing.verticalGap8 = SizedBox(height: 8) AppSpacing.verticalGap12 =
SizedBox(height: 12) AppSpacing.verticalGap16 = SizedBox(height: 16) AppSpacing.verticalGap24 =
SizedBox(height: 24) AppSpacing.horizontalGap6 = SizedBox(width: 6) AppSpacing.horizontalGap8 =
SizedBox(width: 8) AppSpacing.horizontalGap12 = SizedBox(width: 12)

From mobile/lib/src/core/theme/app_durations.dart: AppDurations.pageEntry =
Duration(milliseconds: 800) AppDurations.staggerHeading = Duration(milliseconds: 0) // 0% offset
AppDurations.staggerInput = Duration(milliseconds: 100) // ~12.5% offset
AppDurations.staggerSuggestions = Duration(milliseconds: 200) // ~25% offset

CustomColors extension (from app_colors.dart): final colors =
Theme.of(context).extension<CustomColors>()!; colors.accent // AppColors.accent colors.textPrimary
colors.cardBg // AppColors.secondaryBg colors.openNowText colors.openNowBg colors.openNowBorder
colors.inputFill colors.inputFocusedBorder colors.borderDefault </interfaces> </context>

<tasks>

<task type="auto">
  <name>Task 1: Create DiscoverySearchInput, FilterChipRow, and SuggestionPillRow widgets</name>
  <files>
    mobile/lib/src/modules/home/presentation/ui/widgets/discovery_search_input.dart
    mobile/lib/src/modules/home/presentation/ui/widgets/filter_chip_row.dart
    mobile/lib/src/modules/home/presentation/ui/widgets/suggestion_pill_row.dart
  </files>

<read_first> - web/src/app/page.tsx (web reference — search pill lines 46-95, chip row, trending
list) - mobile/lib/src/core/theme/app_colors.dart (all color constants and CustomColors extension) -
mobile/lib/src/core/theme/app_font_sizes.dart - mobile/lib/src/core/theme/app_radius.dart -
mobile/lib/src/core/theme/app_spacing.dart - mobile/lib/src/core/theme/app_sizes.dart -
.planning/phases/03-discovery-search-screens/03-CONTEXT.md (D-02, D-03, D-04, M-02, M-03) -
.planning/phases/03-discovery-search-screens/03-PATTERNS.md (pattern code for all three widgets)
</read_first>

  <action>
Create three widget files. Import ordering: dart SDK → package:flutter → package:linkbeet/src/core/theme → within-module. All values from theme constants — zero inline hex codes, zero hardcoded font sizes.

**File 1: `mobile/lib/src/modules/home/presentation/ui/widgets/discovery_search_input.dart`**

This widget is used in two contexts:

- Discovery home (full): `compact: false` — `AppRadius.searchInput` (24px), `minHeight: 52`,
  `maxLines: 1` (pill shape matching web page.tsx), `showMic: true`
- Search results (S-03): `compact: true` — `AppRadius.large` (12px), fixed height `44`, single-line,
  `showMic: false`

```dart
import 'package:flutter/material.dart';
import 'package:linkbeet/src/core/theme/app_colors.dart';
import 'package:linkbeet/src/core/theme/app_durations.dart';
import 'package:linkbeet/src/core/theme/app_font_sizes.dart';
import 'package:linkbeet/src/core/theme/app_radius.dart';
import 'package:linkbeet/src/core/theme/app_sizes.dart';
import 'package:linkbeet/src/core/theme/app_spacing.dart';

class DiscoverySearchInput extends StatefulWidget {
  const DiscoverySearchInput({
    super.key,
    this.compact = false,
    this.initialQuery = '',
    this.onQueryChanged,
    this.onSubmitted,
    this.onFocusChanged,
  });

  final bool compact;
  final String initialQuery;
  final ValueChanged<String>? onQueryChanged;
  final ValueChanged<String>? onSubmitted;
  final ValueChanged<bool>? onFocusChanged;

  @override
  State<DiscoverySearchInput> createState() => _DiscoverySearchInputState();
}

class _DiscoverySearchInputState extends State<DiscoverySearchInput> {
  late final TextEditingController _controller;
  late final FocusNode _focusNode;
  bool _isFocused = false;

  @override
  void initState() {
    super.initState();
    _controller = TextEditingController(text: widget.initialQuery);
    _focusNode = FocusNode()
      ..addListener(() {
        setState(() => _isFocused = _focusNode.hasFocus);
        widget.onFocusChanged?.call(_focusNode.hasFocus);
      });
  }

  @override
  void dispose() {
    _controller.dispose();
    _focusNode.dispose();
    super.dispose();
  }

  @override
  Widget build(BuildContext context) {
    final borderRadius = widget.compact ? AppRadius.large : AppRadius.searchInput;
    final minHeight = widget.compact ? 44.0 : 52.0;

    return AnimatedContainer(
      duration: AppDurations.fast,
      constraints: BoxConstraints(minHeight: minHeight),
      decoration: BoxDecoration(
        color: AppColors.inputBg,
        borderRadius: borderRadius,
        border: Border.all(
          color: _isFocused ? AppColors.accentFocusBorder : AppColors.border,
          width: _isFocused ? 1.5 : 1.0,
        ),
        boxShadow: _isFocused ? AppColors.focusShadow : AppColors.cardShadow,
      ),
      child: Row(
        children: [
          // Search icon — left, 16px padding
          Padding(
            padding: const EdgeInsets.only(left: 16),
            child: Icon(
              Icons.search_rounded,
              size: AppSizes.iconLg,
              color: _isFocused ? AppColors.accent : AppColors.textPlaceholder,
            ),
          ),
          AppSpacing.horizontalGap8,
          // Text input
          Expanded(
            child: TextField(
              controller: _controller,
              focusNode: _focusNode,
              maxLines: 1,
              style: const TextStyle(
                fontSize: AppFontSizes.inputText,
                color: AppColors.textPrimary,
              ),
              decoration: const InputDecoration(
                hintText: 'Find a cafe, book a salon, hire a creator...',
                hintStyle: TextStyle(
                  fontSize: AppFontSizes.inputText,
                  color: AppColors.textPlaceholder,
                ),
                border: InputBorder.none,
                enabledBorder: InputBorder.none,
                focusedBorder: InputBorder.none,
                contentPadding: EdgeInsets.symmetric(vertical: 14),
                isDense: true,
              ),
              onChanged: widget.onQueryChanged,
              onSubmitted: widget.onSubmitted,
            ),
          ),
          // Right controls
          Padding(
            padding: const EdgeInsets.only(right: 6),
            child: Row(
              mainAxisSize: MainAxisSize.min,
              children: [
                if (!widget.compact)
                  // Mic button — voice search
                  SizedBox(
                    width: AppSizes.touchTarget,
                    height: AppSizes.touchTarget,
                    child: IconButton(
                      icon: const Icon(Icons.mic_none_rounded),
                      iconSize: AppSizes.iconLg,
                      color: AppColors.textPlaceholder,
                      padding: EdgeInsets.zero,
                      onPressed: () {},
                    ),
                  ),
                AppSpacing.horizontalGap4,
                // Send button — blue when query non-empty
                AnimatedContainer(
                  duration: AppDurations.fast,
                  width: AppSizes.sendButtonSize,
                  height: AppSizes.sendButtonSize,
                  decoration: BoxDecoration(
                    color: _controller.text.isNotEmpty
                        ? AppColors.accent
                        : AppColors.secondaryBg,
                    borderRadius: AppRadius.sendButton,
                  ),
                  child: Material(
                    color: Colors.transparent,
                    child: InkWell(
                      borderRadius: AppRadius.sendButton,
                      onTap: () {
                        if (_controller.text.isNotEmpty) {
                          widget.onSubmitted?.call(_controller.text);
                        }
                      },
                      child: Icon(
                        Icons.arrow_forward_rounded,
                        size: AppSizes.iconLg,
                        color: _controller.text.isNotEmpty
                            ? AppColors.white
                            : AppColors.textPlaceholder,
                      ),
                    ),
                  ),
                ),
              ],
            ),
          ),
        ],
      ),
    );
  }
}
```

**File 2: `mobile/lib/src/modules/home/presentation/ui/widgets/filter_chip_row.dart`**

Horizontal SingleChildScrollView (M-02: no wrap on mobile). Chips: Filters (with icon), separator,
Restaurants, Salons & Spas, Creators, Open Now (emerald).

```dart
import 'package:flutter/material.dart';
import 'package:linkbeet/src/core/theme/app_colors.dart';
import 'package:linkbeet/src/core/theme/app_font_sizes.dart';
import 'package:linkbeet/src/core/theme/app_radius.dart';
import 'package:linkbeet/src/core/theme/app_sizes.dart';
import 'package:linkbeet/src/core/theme/app_spacing.dart';

class FilterChipRow extends StatelessWidget {
  const FilterChipRow({super.key});

  @override
  Widget build(BuildContext context) {
    return SingleChildScrollView(
      scrollDirection: Axis.horizontal,
      padding: AppSpacing.screenPadding,
      child: Row(
        children: [
          _FilterChip(
            label: 'Filters',
            icon: Icons.tune_rounded,
            onTap: () {},
          ),
          AppSpacing.horizontalGap8,
          Container(width: 1, height: 16, color: AppColors.border),
          AppSpacing.horizontalGap8,
          _FilterChip(label: 'Restaurants', onTap: () {}),
          AppSpacing.horizontalGap6,
          _FilterChip(label: 'Salons & Spas', onTap: () {}),
          AppSpacing.horizontalGap6,
          _FilterChip(label: 'Creators', onTap: () {}),
          AppSpacing.horizontalGap6,
          _OpenNowChip(onTap: () {}),
        ],
      ),
    );
  }
}

class _FilterChip extends StatelessWidget {
  const _FilterChip({required this.label, this.icon, required this.onTap});

  final String label;
  final IconData? icon;
  final VoidCallback onTap;

  @override
  Widget build(BuildContext context) {
    return GestureDetector(
      onTap: onTap,
      child: Container(
        padding: AppSpacing.filterChipPadding,
        decoration: BoxDecoration(
          color: AppColors.pageBg,
          borderRadius: AppRadius.pill,
          border: Border.all(color: AppColors.border),
        ),
        child: Row(
          mainAxisSize: MainAxisSize.min,
          children: [
            if (icon != null) ...[
              Icon(icon, size: AppSizes.iconSm, color: AppColors.textSecondary),
              AppSpacing.horizontalGap6,
            ],
            Text(
              label,
              style: const TextStyle(
                fontSize: AppFontSizes.filterChip,
                fontWeight: FontWeight.w500,
                color: AppColors.textSecondary,
              ),
            ),
          ],
        ),
      ),
    );
  }
}

class _OpenNowChip extends StatelessWidget {
  const _OpenNowChip({required this.onTap});

  final VoidCallback onTap;

  @override
  Widget build(BuildContext context) {
    return GestureDetector(
      onTap: onTap,
      child: Container(
        padding: AppSpacing.filterChipPadding,
        decoration: BoxDecoration(
          color: AppColors.openNowBg,
          borderRadius: AppRadius.pill,
          border: Border.all(color: AppColors.openNowBorder),
        ),
        child: const Text(
          'Open Now',
          style: TextStyle(
            fontSize: AppFontSizes.filterChip,
            fontWeight: FontWeight.w500,
            color: AppColors.openNowText,
          ),
        ),
      ),
    );
  }
}
```

**File 3: `mobile/lib/src/modules/home/presentation/ui/widgets/suggestion_pill_row.dart`**

Horizontal ScrollView, 3 pills (M-03 — 2 visible, rest scrollable). Icon colors: success
(emerald-500), const Color(0xFFA855F7) (purple-500 — one-off not in AppColors), accent (blue).

```dart
import 'package:flutter/material.dart';
import 'package:linkbeet/src/core/theme/app_colors.dart';
import 'package:linkbeet/src/core/theme/app_font_sizes.dart';
import 'package:linkbeet/src/core/theme/app_radius.dart';
import 'package:linkbeet/src/core/theme/app_sizes.dart';
import 'package:linkbeet/src/core/theme/app_spacing.dart';

class SuggestionPillRow extends StatelessWidget {
  const SuggestionPillRow({super.key});

  @override
  Widget build(BuildContext context) {
    return SingleChildScrollView(
      scrollDirection: Axis.horizontal,
      padding: AppSpacing.screenPadding,
      child: Row(
        children: [
          _SuggestionPill(
            icon: Icons.location_on_outlined,
            iconColor: AppColors.success,
            label: 'Top-rated near me',
            onTap: () {},
          ),
          AppSpacing.horizontalGap8,
          _SuggestionPill(
            icon: Icons.auto_awesome_rounded,
            iconColor: const Color(0xFFA855F7), // purple-500 — one-off, not in AppColors
            label: 'Featured this week',
            onTap: () {},
          ),
          AppSpacing.horizontalGap8,
          _SuggestionPill(
            icon: Icons.search_rounded,
            iconColor: AppColors.accent,
            label: 'Search by category',
            onTap: () {},
          ),
        ],
      ),
    );
  }
}

class _SuggestionPill extends StatelessWidget {
  const _SuggestionPill({
    required this.icon,
    required this.iconColor,
    required this.label,
    required this.onTap,
  });

  final IconData icon;
  final Color iconColor;
  final String label;
  final VoidCallback onTap;

  @override
  Widget build(BuildContext context) {
    return GestureDetector(
      onTap: onTap,
      child: Container(
        padding: AppSpacing.suggestionPillPadding,
        decoration: BoxDecoration(
          color: AppColors.pageBg,
          borderRadius: AppRadius.large,
          border: Border.all(color: AppColors.border),
          boxShadow: AppColors.cardShadow,
        ),
        child: Row(
          mainAxisSize: MainAxisSize.min,
          children: [
            Icon(icon, size: AppSizes.iconSm, color: iconColor),
            AppSpacing.horizontalGap8,
            Text(
              label,
              style: const TextStyle(
                fontSize: AppFontSizes.suggestionPill,
                fontWeight: FontWeight.w500,
                color: AppColors.textSecondary,
              ),
            ),
          ],
        ),
      ),
    );
  }
}
```

  </action>

  <verify>
    <automated>
      grep -n "DiscoverySearchInput" /Users/nidhin/My/DEVELOPMENT/BUSSINESS/WEB/linkbeet-v2/mobile/lib/src/modules/home/presentation/ui/widgets/discovery_search_input.dart
      grep -n "FilterChipRow" /Users/nidhin/My/DEVELOPMENT/BUSSINESS/WEB/linkbeet-v2/mobile/lib/src/modules/home/presentation/ui/widgets/filter_chip_row.dart
      grep -n "SuggestionPillRow" /Users/nidhin/My/DEVELOPMENT/BUSSINESS/WEB/linkbeet-v2/mobile/lib/src/modules/home/presentation/ui/widgets/suggestion_pill_row.dart
      grep -rn "0xFF0071" /Users/nidhin/My/DEVELOPMENT/BUSSINESS/WEB/linkbeet-v2/mobile/lib/src/modules/home/presentation/ui/widgets/ || echo "PASS: no inline accent hex in widgets"
      cd /Users/nidhin/My/DEVELOPMENT/BUSSINESS/WEB/linkbeet-v2/mobile && flutter analyze lib/src/modules/home/presentation/ui/widgets/
    </automated>
  </verify>

<acceptance_criteria> - `discovery_search_input.dart` contains
`class DiscoverySearchInput extends StatefulWidget` - `discovery_search_input.dart` contains
`compact = false` default parameter - `discovery_search_input.dart` contains
`AppColors.accentFocusBorder` (focus border color) - `discovery_search_input.dart` contains
`AppRadius.searchInput` (non-compact) and `AppRadius.large` (compact) -
`discovery_search_input.dart` contains `AppColors.focusShadow` and `AppColors.cardShadow` -
`discovery_search_input.dart` contains `AppSizes.sendButtonSize` (not hardcoded `40`) -
`filter_chip_row.dart` contains `class FilterChipRow extends StatelessWidget` -
`filter_chip_row.dart` contains `scrollDirection: Axis.horizontal` - `filter_chip_row.dart` contains
`AppColors.openNowBg` (emerald chip) - `filter_chip_row.dart` contains `AppColors.openNowText` -
`filter_chip_row.dart` contains `AppRadius.pill` (chip shape) - `suggestion_pill_row.dart` contains
`class SuggestionPillRow extends StatelessWidget` - `suggestion_pill_row.dart` contains
`scrollDirection: Axis.horizontal` - `suggestion_pill_row.dart` contains `AppColors.success`
(emerald-500 location icon) - `suggestion_pill_row.dart` contains `const Color(0xFFA855F7)`
(purple-500 one-off) - `suggestion_pill_row.dart` contains `AppRadius.large` (pill border radius) -
No file contains any raw hex string other than the purple-500 one-off:
`grep -rn "0xFF[0-9A-F]" widgets/` should only match `0xFFA855F7` -
`flutter analyze lib/src/modules/home/presentation/ui/widgets/` reports zero errors
</acceptance_criteria>

  <done>
    Three widget files exist. Each declares its primary exported class. No inline hex colors except the purple-500 one-off in suggestion_pill_row.dart. flutter analyze reports zero errors on all three files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement HomeScreen — full Discovery home with branding, trending list, and bottom-floating search (M-01)</name>
  <files>
    mobile/lib/src/modules/home/presentation/ui/screens/home_screen.dart
  </files>

<read_first> - mobile/lib/src/modules/home/presentation/ui/screens/home_screen.dart (current stub —
9 lines) - mobile/lib/src/modules/foundation/app/presentation/ui/screens/splash_screen.dart
(AnimationController + stagger pattern) - web/src/app/page.tsx (full web reference — logo block,
search pill, trending list structure) -
mobile/lib/src/modules/home/presentation/ui/widgets/discovery_search_input.dart (widget created in
Task 1) - mobile/lib/src/modules/home/presentation/ui/widgets/filter_chip_row.dart (widget created
in Task 1) - mobile/lib/src/modules/home/presentation/ui/widgets/suggestion_pill_row.dart (widget
created in Task 1) - mobile/lib/src/core/theme/app_durations.dart (pageEntry=800ms, stagger
offsets) - mobile/lib/src/core/theme/app_colors.dart -
mobile/lib/src/core/theme/app_font_sizes.dart - mobile/lib/src/core/router/app_routes.dart (for
navigation to search) - .planning/phases/03-discovery-search-screens/03-CONTEXT.md (D-01, M-01,
M-02, M-03 specs) - .planning/phases/03-discovery-search-screens/03-PATTERNS.md (home_screen pattern
section) </read_first>

  <action>
Replace the 9-line stub `home_screen.dart` entirely. The screen replicates `web/src/app/page.tsx` for mobile using the M-01 bottom-floating search pattern.

**Layout structure (M-01 per CONTEXT.md):**

- `Scaffold` with `body: SafeArea(child: Stack([...]))`
- Stack child 1: scrollable center content (logo, trending list, filter chips, suggestion pills)
- Stack child 2: `Positioned(bottom: 16, left: 16, right: 16, child: DiscoverySearchInput(...))`

**Animation pattern** (from splash_screen.dart — use `TickerProviderStateMixin` NOT
`SingleTickerProviderStateMixin` because we have 3 animations):

- One `AnimationController` with `duration: AppDurations.pageEntry` (800ms)
- Three `CurvedAnimation` objects using `Interval`:
  - `_logoAnim`: `Interval(0.0, 0.6, curve: Curves.easeOut)` — staggerHeading (0ms)
  - `_chipsAnim`: `Interval(0.1, 0.7, curve: Curves.easeOut)` — staggerInput (100ms)
  - `_suggestionsAnim`: `Interval(0.2, 0.8, curve: Curves.easeOut)` — staggerSuggestions (200ms)
- Wrap each section with:
  ```dart
  FadeTransition(
    opacity: _logoAnim,
    child: SlideTransition(
      position: Tween<Offset>(begin: const Offset(0, 0.15), end: Offset.zero).animate(_logoAnim),
      child: ...,
    ),
  )
  ```

**Logo + wordmark block** (from web page.tsx lines 29-42):

```dart
Row(
  mainAxisSize: MainAxisSize.min,
  children: [
    // Logo image — black-logo.png (48px on web, use 40px on mobile)
    Image.asset(
      'assets/images/black-logo.png',
      width: 40,
      height: 40,
      fit: BoxFit.contain,
    ),
    AppSpacing.horizontalGap12,
    const Text(
      'LinkBeet',
      style: TextStyle(
        fontSize: 36,   // web uses 36px-52px range; 36px on mobile
        fontWeight: FontWeight.w500,
        color: AppColors.textPrimary,
        letterSpacing: -0.5,
        height: 1.0,
      ),
    ),
  ],
)
```

**Trending searches list** (from web page.tsx TRENDING array + list rendering):

```dart
// Static TRENDING data — mock only, no backend
const _kTrending = [
  'Top-rated coffee shops near me',
  'Affordable salons open now',
  'Street food vendors around me',
  'Event photographers in the city',
  'Fitness trainers accepting clients',
  'Digital creators for brand promos',
  'Budget-friendly co-working spaces',
];

// Section header:
const Text(
  'Trending searches',
  style: TextStyle(
    fontSize: AppFontSizes.filterChip,  // 13px
    fontWeight: FontWeight.w600,
    color: AppColors.textTertiary,
    letterSpacing: 0.5,
  ),
)

// Each trending item — ListView.builder or Column of items:
GestureDetector(
  onTap: () { /* set query */ },
  child: Padding(
    padding: const EdgeInsets.symmetric(vertical: 14),
    child: Row(
      children: [
        Container(
          width: 32,
          height: 32,
          decoration: BoxDecoration(
            color: AppColors.secondaryBg,
            borderRadius: AppRadius.pill,
          ),
          child: const Icon(
            Icons.trending_up_rounded,
            size: AppSizes.iconSm,
            color: AppColors.accent,
          ),
        ),
        AppSpacing.horizontalGap12,
        Expanded(
          child: Text(
            label,
            style: const TextStyle(
              fontSize: AppFontSizes.bodySmall,  // 15px
              color: AppColors.textSecondary,
            ),
          ),
        ),
      ],
    ),
  ),
)
// Separator between items (not after last):
if (index < _kTrending.length - 1)
  Divider(height: 1, color: AppColors.border, indent: 44),
```

**Complete screen structure:**

```dart
Scaffold(
  // backgroundColor inherited from AppTheme.lightTheme — do NOT set explicitly
  body: SafeArea(
    child: Stack(
      children: [
        // ── Scrollable main content ────────────────────────
        SingleChildScrollView(
          padding: const EdgeInsets.fromLTRB(16, 32, 16, 120), // bottom: 120 = space for floating input
          child: Column(
            crossAxisAlignment: CrossAxisAlignment.start,
            children: [
              // Logo + wordmark (FadeTransition + SlideTransition with _logoAnim)
              Center(child: ..logoBlock..),
              AppSpacing.verticalGap32,
              // Trending searches section header + list (FadeTransition with _logoAnim)
              const Text('Trending searches', style: ...),
              AppSpacing.verticalGap8,
              Column(children: _kTrending.asMap().entries.map(...).toList()),
              AppSpacing.verticalGap24,
              // Filter chips row (FadeTransition with _chipsAnim) — negative margin to break out of padding
              FadeTransition(
                opacity: _chipsAnim,
                child: SlideTransition(
                  position: Tween<Offset>(...).animate(_chipsAnim),
                  child: const FilterChipRow(),  // FilterChipRow has its own padding
                ),
              ),
              AppSpacing.verticalGap12,
              // Suggestion pills row (FadeTransition with _suggestionsAnim)
              FadeTransition(
                opacity: _suggestionsAnim,
                child: SlideTransition(
                  position: Tween<Offset>(...).animate(_suggestionsAnim),
                  child: const SuggestionPillRow(),
                ),
              ),
            ],
          ),
        ),
        // ── Floating bottom search input (M-01) ─────────────
        Positioned(
          bottom: 16,
          left: 16,
          right: 16,
          child: DiscoverySearchInput(
            onSubmitted: (query) {
              if (query.isNotEmpty) {
                // Navigate to search results — route wiring done in plan 04
                // For now: no-op until SearchResultsRoute exists
              }
            },
          ),
        ),
      ],
    ),
  ),
)
```

**Import section** (follow project import ordering convention):

```dart
import 'package:flutter/material.dart';
import 'package:linkbeet/src/core/theme/app_colors.dart';
import 'package:linkbeet/src/core/theme/app_durations.dart';
import 'package:linkbeet/src/core/theme/app_font_sizes.dart';
import 'package:linkbeet/src/core/theme/app_radius.dart';
import 'package:linkbeet/src/core/theme/app_sizes.dart';
import 'package:linkbeet/src/core/theme/app_spacing.dart';
import 'package:linkbeet/src/modules/home/presentation/ui/widgets/discovery_search_input.dart';
import 'package:linkbeet/src/modules/home/presentation/ui/widgets/filter_chip_row.dart';
import 'package:linkbeet/src/modules/home/presentation/ui/widgets/suggestion_pill_row.dart';
```

Use `TickerProviderStateMixin` (not `SingleTickerProviderStateMixin`) since there are multiple
CurvedAnimations sharing one controller. The controller itself is one — just the curves are derived
from it with `Interval`.

The `_trendingSearchQuery` state variable should hold the current query so tapping a trending item
sets the text in the floating search input. Use a `GlobalKey<_DiscoverySearchInputState>` OR simply
manage the `TextEditingController` at the screen level and pass it down. Simpler approach: create
`TextEditingController _searchController` in the screen state and pass it to `DiscoverySearchInput`
via a new optional `controller` parameter added to the widget.

Actually, for simplicity and to avoid over-engineering: the trending items are tappable but only
update local state for now (no navigation until plan 04 wires search results). Add `_searchQuery`
state string updated by onQueryChanged callback. </action>

  <verify>
    <automated>
      grep -n "DiscoverySearchInput\|FilterChipRow\|SuggestionPillRow" /Users/nidhin/My/DEVELOPMENT/BUSSINESS/WEB/linkbeet-v2/mobile/lib/src/modules/home/presentation/ui/screens/home_screen.dart
      grep -n "Positioned" /Users/nidhin/My/DEVELOPMENT/BUSSINESS/WEB/linkbeet-v2/mobile/lib/src/modules/home/presentation/ui/screens/home_screen.dart
      grep -n "AnimationController\|AppDurations.pageEntry" /Users/nidhin/My/DEVELOPMENT/BUSSINESS/WEB/linkbeet-v2/mobile/lib/src/modules/home/presentation/ui/screens/home_screen.dart
      grep -n "AppDurations\|AppColors\|AppFontSizes\|AppRadius\|AppSpacing\|AppSizes" /Users/nidhin/My/DEVELOPMENT/BUSSINESS/WEB/linkbeet-v2/mobile/lib/src/modules/home/presentation/ui/screens/home_screen.dart | wc -l
      grep -n "Text('Home')" /Users/nidhin/My/DEVELOPMENT/BUSSINESS/WEB/linkbeet-v2/mobile/lib/src/modules/home/presentation/ui/screens/home_screen.dart || echo "PASS: stub removed"
      cd /Users/nidhin/My/DEVELOPMENT/BUSSINESS/WEB/linkbeet-v2/mobile && flutter analyze lib/src/modules/home/
    </automated>
  </verify>

<acceptance_criteria> - `home_screen.dart` does NOT contain `Text('Home')` (stub removed) -
`home_screen.dart` contains `class HomeScreen extends StatefulWidget` - `home_screen.dart` contains
`TickerProviderStateMixin` - `home_screen.dart` contains `AnimationController` with
`AppDurations.pageEntry` - `home_screen.dart` contains `Interval(0.0, 0.6` (logo animation
interval) - `home_screen.dart` contains `Interval(0.1, 0.7` (chips animation interval) -
`home_screen.dart` contains `Interval(0.2, 0.8` (suggestions animation interval) -
`home_screen.dart` contains `FadeTransition` (at least once) - `home_screen.dart` contains
`SlideTransition` (at least once) - `home_screen.dart` contains `Stack(` and `Positioned(` (M-01
bottom-floating pattern) - `home_screen.dart` contains `bottom: 16` inside a `Positioned` -
`home_screen.dart` contains `DiscoverySearchInput(` - `home_screen.dart` contains `FilterChipRow(` -
`home_screen.dart` contains `SuggestionPillRow(` - `home_screen.dart` contains `LinkBeet` text for
wordmark - `home_screen.dart` contains `AppDurations.pageEntry` - `home_screen.dart` does NOT
contain any raw hex color code (no `0xFF[0-9A-Fa-f]{6}` other than theme constant references) -
`flutter analyze lib/src/modules/home/` reports zero errors </acceptance_criteria>

  <done>
    HomeScreen is a full StatefulWidget replacing the Text('Home') stub. It shows the LinkBeet logo+wordmark, trending searches list, filter chips, suggestion pills, and a bottom-floating search input — all with staggered fadeInUp entry animations. flutter analyze reports zero errors.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                  | Description                                                                   |
| ------------------------- | ----------------------------------------------------------------------------- |
| User input → search query | Text entered in DiscoverySearchInput crosses from untrusted user to app state |

## STRIDE Threat Register

| Threat ID  | Category               | Component                         | Disposition | Mitigation Plan                                                                                             |
| ---------- | ---------------------- | --------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------- |
| T-03-03-01 | Tampering              | DiscoverySearchInput query text   | accept      | UI-only phase, no backend calls — query used only for local navigation. Validated at API layer in Phase 13. |
| T-03-03-02 | DoS                    | AnimationController not disposed  | mitigate    | Implement `dispose()` method calling `_controller.dispose()` in HomeScreen state. Verified by analyzer.     |
| T-03-03-03 | Information Disclosure | Trending search strings hardcoded | accept      | Mock data only — no PII, no secrets. Will be replaced by real API in Phase 13.                              |

</threat_model>

<verification>
1. Run `cd /Users/nidhin/My/DEVELOPMENT/BUSSINESS/WEB/linkbeet-v2/mobile && flutter analyze lib/src/modules/home/` — zero errors
2. Verify `home_screen.dart` no longer contains `Text('Home')`: `grep "Text('Home')" mobile/lib/src/modules/home/presentation/ui/screens/home_screen.dart` — returns empty
3. Verify three widget files created: `ls mobile/lib/src/modules/home/presentation/ui/widgets/` — shows discovery_search_input.dart, filter_chip_row.dart, suggestion_pill_row.dart
4. Verify no inline hex in widget files (excluding purple-500 one-off): `grep -rn "0xFF[0-9A-F]\{6\}" mobile/lib/src/modules/home/presentation/ui/widgets/` — only `0xFFA855F7` should appear
5. Verify M-01 pattern: `grep "Positioned" mobile/lib/src/modules/home/presentation/ui/screens/home_screen.dart` — finds `Positioned(`
6. Verify animation constants used: `grep "AppDurations" mobile/lib/src/modules/home/presentation/ui/screens/home_screen.dart` — finds references
</verification>

<success_criteria>

- [ ] `discovery_search_input.dart` exists with focus-animated border (accentFocusBorder), mic icon,
      send button (blue when non-empty), compact mode param
- [ ] `filter_chip_row.dart` exists with horizontal SingleChildScrollView, 5 chips including emerald
      "Open Now"
- [ ] `suggestion_pill_row.dart` exists with horizontal scroll, 3 pills with
      location/sparkles/search icons
- [ ] `home_screen.dart` fully replaces stub — StatefulWidget with AnimationController
- [ ] Bottom-floating search input is Positioned(bottom: 16) inside Stack (M-01)
- [ ] Three staggered animations using Interval(0.0...), Interval(0.1...), Interval(0.2...)
- [ ] Logo + "LinkBeet" wordmark centered at top
- [ ] Trending searches list visible below logo
- [ ] flutter analyze reports zero errors on entire home module </success_criteria>

<output>
After completion, create `.planning/phases/03-discovery-search-screens/03-03-SUMMARY.md` summarizing:
- Files created (paths)
- Key implementation decisions (e.g., TickerProviderStateMixin, controller parameter approach)
- Any deviations from the plan and why
- Remaining known gaps
</output>
