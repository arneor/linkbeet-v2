---
phase: 3
plan: 02
type: feature
wave: 1
depends_on: []
files_modified:
  - mobile/lib/src/core/ui/theme/app_colors.dart
  - mobile/lib/src/core/ui/theme/app_typography.dart
  - mobile/lib/src/core/ui/theme/app_theme.dart
  - mobile/lib/src/core/ui/theme/app_spacing.dart
  - mobile/lib/src/core/ui/theme/app_shadows.dart
  - mobile/lib/src/core/ui/theme/app_radius.dart
  - mobile/lib/src/core/domain/errors/failures.dart
  - mobile/lib/src/core/domain/usecases/usecase.dart
  - mobile/lib/src/core/di/injection.dart
  - mobile/lib/src/core/router/app_router.dart
  - mobile/lib/src/core/ui/widgets/lb_button.dart
  - mobile/lib/src/core/ui/widgets/lb_text_field.dart
  - mobile/lib/src/core/ui/widgets/lb_card.dart
  - mobile/lib/src/core/ui/widgets/lb_badge.dart
  - mobile/lib/src/core/ui/widgets/lb_avatar.dart
  - mobile/lib/src/core/ui/widgets/lb_loading.dart
  - mobile/lib/src/core/ui/widgets/lb_chip.dart
  - mobile/lib/src/core/ui/widgets/lb_empty_state.dart
  - mobile/lib/src/core/ui/widgets/navigation/lb_drawer.dart
  - mobile/lib/src/core/ui/widgets/navigation/lb_app_bar.dart
  - mobile/lib/src/core/ui/widgets/navigation/lb_app_shell.dart
  - mobile/lib/main.dart
autonomous: true
requirements:
  - MOB-04
  - MOB-05
---

<objective>
Build the core design system, theme, shared widget library, navigation shell, dependency injection
setup, and app routing — all matching the locked "Spatial AI Light" design system from DESIGN.md.
This is the Flutter equivalent of Phase 1's design tokens + shared components, adapted for mobile.
</objective>

<tasks>

## Task 1: Create App Color Tokens

<type>code</type>

<read_first>

- DESIGN.md (Section 1: Color System — complete token reference) </read_first>

<files>
- mobile/lib/src/core/ui/theme/app_colors.dart
</files>

<action>
Create `mobile/lib/src/core/ui/theme/app_colors.dart`:

```dart
import 'package:flutter/material.dart';

/// LinkBeet Design System — Color Tokens
/// Source of truth: DESIGN.md "Spatial AI Light" theme
/// Apple Blue (#0071e3) is the ONLY chromatic accent color.
abstract final class AppColors {
  // === Backgrounds ===
  static const Color pageBg = Color(0xFFFFFFFF);
  static const Color secondaryBg = Color(0xFFF5F5F7);
  static const Color sidebarBg = Color(0xE6F9FAFB); // slate-50/90
  static const Color inputBg = Color(0xFFFFFFFF);

  // === Text ===
  static const Color textPrimary = Color(0xFF1D1D1F); // slate-900
  static const Color textSecondary = Color(0xFF475569); // slate-600
  static const Color textTertiary = Color(0xFF64748B); // slate-500
  static const Color textPlaceholder = Color(0xFFCBD5E1); // slate-300
  static const Color textInverse = Color(0xFFFFFFFF);

  // === Interactive — Apple Blue ONLY ===
  static const Color accent = Color(0xFF0071E3);
  static const Color accentHover = Color(0xFF0077ED);
  static const Color accentTintBg = Color(0x1A0071E3); // 10% opacity
  static const Color accentTintRing = Color(0x0D0071E3); // 5% opacity
  static const Color accentFocusBorder = Color(0x660071E3); // 40% opacity
  static const Color linkLight = Color(0xFF0066CC);

  // === Borders & Dividers ===
  static const Color border = Color(0xFFE2E8F0); // slate-200
  static const Color divider = Color(0xFFE2E8F0); // slate-200

  // === Special Accent (sparingly) ===
  static const Color emeraldText = Color(0xFF15803D); // emerald-700
  static const Color emeraldBg = Color(0xFFF0FDF4); // emerald-50
  static const Color emeraldBorder = Color(0xFFA7F3D0); // emerald-200

  // === Suggestion Pill Icon Colors ===
  static const Color iconEmerald = Color(0xFF10B981); // emerald-500
  static const Color iconPurple = Color(0xFFA855F7); // purple-500
  static const Color iconBlue = Color(0xFF0071E3); // same as accent

  // === Shadows (used as Color values for Shadow widgets) ===
  static const Color shadowDefault = Color(0x0F000000); // rgba(0,0,0,0.06)
  static const Color shadowFocus = Color(0x260071E3); // rgba(0,113,227,0.15)

  // === Overlay ===
  static const Color drawerOverlay = Color(0x80000000); // black/50
}
```

</action>

<acceptance_criteria>

- `app_colors.dart` contains `static const Color accent = Color(0xFF0071E3)`
- `app_colors.dart` contains `static const Color pageBg = Color(0xFFFFFFFF)`
- `app_colors.dart` contains `static const Color secondaryBg = Color(0xFFF5F5F7)`
- `app_colors.dart` contains `static const Color textPrimary = Color(0xFF1D1D1F)`
- `app_colors.dart` contains `abstract final class AppColors`
- No other chromatic accent colors defined (only blue + emerald for "Open Now" + purple for icons)
  </acceptance_criteria>

---

## Task 2: Create Typography Tokens

<type>code</type>

<read_first>

- DESIGN.md (Section 2: Typography — scale, weights, letter-spacing) </read_first>

<files>
- mobile/lib/src/core/ui/theme/app_typography.dart
</files>

<action>
Create `mobile/lib/src/core/ui/theme/app_typography.dart`:

```dart
import 'package:flutter/material.dart';
import 'package:google_fonts/google_fonts.dart';

import 'app_colors.dart';

/// LinkBeet Design System — Typography Scale
/// Mobile uses system font (SF Pro on iOS, Roboto on Android) per DESIGN.md.
/// Google Fonts Inter as fallback for consistency with web.
abstract final class AppTypography {
  static TextStyle get _baseStyle => GoogleFonts.inter(
        color: AppColors.textPrimary,
      );

  // === Display ===
  static TextStyle get heroGreeting => _baseStyle.copyWith(
        fontSize: 30, // 30px on mobile (44px on web desktop)
        fontWeight: FontWeight.w500,
        height: 1.15,
        letterSpacing: -0.5,
      );

  static TextStyle get sectionHeading => _baseStyle.copyWith(
        fontSize: 28,
        fontWeight: FontWeight.w600,
        height: 1.10,
        letterSpacing: -0.5,
      );

  // === Headings ===
  static TextStyle get tileHeading => _baseStyle.copyWith(
        fontSize: 22,
        fontWeight: FontWeight.w400,
        height: 1.14,
        letterSpacing: 0.196,
      );

  static TextStyle get subHeading => _baseStyle.copyWith(
        fontSize: 18,
        fontWeight: FontWeight.w400,
        height: 1.19,
        letterSpacing: 0.231,
      );

  // === Body ===
  static TextStyle get body => _baseStyle.copyWith(
        fontSize: 17,
        fontWeight: FontWeight.w400,
        height: 1.47,
        letterSpacing: -0.374,
      );

  static TextStyle get bodySecondary => body.copyWith(
        color: AppColors.textSecondary,
      );

  // === Navigation ===
  static TextStyle get navLabel => _baseStyle.copyWith(
        fontSize: 12,
        fontWeight: FontWeight.w400,
        color: AppColors.textSecondary,
      );

  static TextStyle get navLabelActive => navLabel.copyWith(
        color: AppColors.accent,
      );

  // === Input ===
  static TextStyle get inputText => _baseStyle.copyWith(
        fontSize: 17,
        fontWeight: FontWeight.w400,
        height: 1.5,
        letterSpacing: -0.5,
        color: AppColors.textPrimary,
      );

  static TextStyle get inputPlaceholder => inputText.copyWith(
        color: AppColors.textPlaceholder,
      );

  // === Chips & Pills ===
  static TextStyle get filterChip => _baseStyle.copyWith(
        fontSize: 13,
        fontWeight: FontWeight.w500,
        color: AppColors.textSecondary,
      );

  static TextStyle get suggestionPill => _baseStyle.copyWith(
        fontSize: 14,
        fontWeight: FontWeight.w500,
        color: AppColors.textSecondary,
      );

  // === Buttons ===
  static TextStyle get buttonPrimary => _baseStyle.copyWith(
        fontSize: 14,
        fontWeight: FontWeight.w400,
        color: AppColors.textInverse,
      );

  static TextStyle get buttonGhost => _baseStyle.copyWith(
        fontSize: 14,
        fontWeight: FontWeight.w400,
        color: AppColors.textSecondary,
      );

  // === Captions & Micro ===
  static TextStyle get caption => _baseStyle.copyWith(
        fontSize: 14,
        fontWeight: FontWeight.w400,
        height: 1.29,
        letterSpacing: -0.224,
        color: AppColors.textSecondary,
      );

  static TextStyle get micro => _baseStyle.copyWith(
        fontSize: 12,
        fontWeight: FontWeight.w400,
        height: 1.33,
        letterSpacing: -0.12,
        color: AppColors.textTertiary,
      );

  // === Badge ===
  static TextStyle get badge => _baseStyle.copyWith(
        fontSize: 11,
        fontWeight: FontWeight.w500,
      );
}
```

</action>

<acceptance_criteria>

- `app_typography.dart` contains `GoogleFonts.inter`
- `app_typography.dart` contains `heroGreeting` with `fontSize: 30`
- `app_typography.dart` contains `filterChip` with `fontSize: 13`
- `app_typography.dart` contains `abstract final class AppTypography`
- All text styles reference `AppColors` for color values </acceptance_criteria>

---

## Task 3: Create Spacing, Radius & Shadow Tokens

<type>code</type>

<read_first>

- DESIGN.md (Sections 6, 7: Border Radius Scale, Spacing System) </read_first>

<files>
- mobile/lib/src/core/ui/theme/app_spacing.dart
- mobile/lib/src/core/ui/theme/app_radius.dart
- mobile/lib/src/core/ui/theme/app_shadows.dart
</files>

<action>
Create `mobile/lib/src/core/ui/theme/app_spacing.dart`:

```dart
/// LinkBeet Design System — Spacing System (base unit: 8px)
abstract final class AppSpacing {
  static const double xs = 4;
  static const double sm = 8;
  static const double md = 12;
  static const double lg = 16;
  static const double xl = 24;
  static const double xxl = 32;
  static const double xxxl = 48;

  // Named measurements from DESIGN.md
  static const double sidebarWidth = 260;
  static const double sidebarCollapsed = 72;
  static const double navItemHeight = 48;
  static const double topBarHeight = 48;
  static const double logoBadgeSize = 32;
  static const double avatarSize = 32;
  static const double contentMaxWidth = 980;
  static const double contentPaddingH = 16; // px-4
  static const double contentPaddingV = 24; // py-6
  static const double touchTargetMin = 44; // iOS HIG minimum
}
```

Create `mobile/lib/src/core/ui/theme/app_radius.dart`:

```dart
import 'package:flutter/material.dart';

/// LinkBeet Design System — Border Radius Scale
abstract final class AppRadius {
  static const double micro = 5;
  static const double standard = 8;
  static const double comfortable = 11;
  static const double large = 12;
  static const double searchInput = 24;
  static const double sendButton = 20;
  static const double pill = 980; // "Learn more" pill links
  static const double circle = 999; // Full circle (chips, avatars)

  // Convenience BorderRadius objects
  static final BorderRadius microRadius = BorderRadius.circular(micro);
  static final BorderRadius standardRadius = BorderRadius.circular(standard);
  static final BorderRadius comfortableRadius = BorderRadius.circular(comfortable);
  static final BorderRadius largeRadius = BorderRadius.circular(large);
  static final BorderRadius searchRadius = BorderRadius.circular(searchInput);
  static final BorderRadius sendRadius = BorderRadius.circular(sendButton);
  static final BorderRadius pillRadius = BorderRadius.circular(pill);
  static final BorderRadius circleRadius = BorderRadius.circular(circle);
}
```

Create `mobile/lib/src/core/ui/theme/app_shadows.dart`:

```dart
import 'package:flutter/material.dart';

import 'app_colors.dart';

/// LinkBeet Design System — Shadow Tokens
abstract final class AppShadows {
  /// Default card shadow: 0 8px 30px rgba(0,0,0,0.06)
  static const List<BoxShadow> card = [
    BoxShadow(
      color: AppColors.shadowDefault,
      offset: Offset(0, 8),
      blurRadius: 30,
    ),
  ];

  /// Focus shadow: 0 4px 30px rgba(0,113,227,0.15)
  static const List<BoxShadow> focus = [
    BoxShadow(
      color: AppColors.shadowFocus,
      offset: Offset(0, 4),
      blurRadius: 30,
    ),
  ];

  /// Small shadow for chips/pills
  static const List<BoxShadow> sm = [
    BoxShadow(
      color: Color(0x0A000000),
      offset: Offset(0, 1),
      blurRadius: 3,
    ),
    BoxShadow(
      color: Color(0x0A000000),
      offset: Offset(0, 1),
      blurRadius: 2,
    ),
  ];
}
```

</action>

<acceptance_criteria>

- `app_spacing.dart` contains `static const double touchTargetMin = 44`
- `app_radius.dart` contains `static const double searchInput = 24`
- `app_radius.dart` contains `BorderRadius.circular(standard)`
- `app_shadows.dart` contains `AppColors.shadowDefault`
- `app_shadows.dart` contains `AppColors.shadowFocus` </acceptance_criteria>

---

## Task 4: Create AppTheme (ThemeData)

<type>code</type>

<read_first>

- mobile/lib/src/core/ui/theme/app_colors.dart
- mobile/lib/src/core/ui/theme/app_typography.dart
- DESIGN.md (Section 9: Hard Rules — no dark mode) </read_first>

<files>
- mobile/lib/src/core/ui/theme/app_theme.dart
</files>

<action>
Create `mobile/lib/src/core/ui/theme/app_theme.dart`:

```dart
import 'package:flutter/material.dart';
import 'package:flutter/services.dart';

import 'app_colors.dart';
import 'app_radius.dart';
import 'app_typography.dart';

/// LinkBeet AppTheme — "Spatial AI Light" (light mode ONLY, per DESIGN.md)
abstract final class AppTheme {
  static ThemeData get light => ThemeData(
        useMaterial3: true,
        brightness: Brightness.light,
        scaffoldBackgroundColor: AppColors.pageBg,
        colorScheme: const ColorScheme.light(
          primary: AppColors.accent,
          onPrimary: AppColors.textInverse,
          secondary: AppColors.secondaryBg,
          onSecondary: AppColors.textPrimary,
          surface: AppColors.pageBg,
          onSurface: AppColors.textPrimary,
          error: Color(0xFFDC2626), // red-600
          outline: AppColors.border,
        ),
        // AppBar
        appBarTheme: AppBarTheme(
          elevation: 0,
          scrolledUnderElevation: 0,
          backgroundColor: AppColors.pageBg,
          foregroundColor: AppColors.textPrimary,
          systemOverlayStyle: SystemUiOverlayStyle.dark,
          titleTextStyle: AppTypography.subHeading,
          centerTitle: false,
        ),
        // Input / TextField
        inputDecorationTheme: InputDecorationTheme(
          filled: true,
          fillColor: AppColors.inputBg,
          border: OutlineInputBorder(
            borderRadius: AppRadius.standardRadius,
            borderSide: const BorderSide(color: AppColors.border),
          ),
          enabledBorder: OutlineInputBorder(
            borderRadius: AppRadius.standardRadius,
            borderSide: const BorderSide(color: AppColors.border),
          ),
          focusedBorder: OutlineInputBorder(
            borderRadius: AppRadius.standardRadius,
            borderSide: const BorderSide(
              color: AppColors.accentFocusBorder,
              width: 1.5,
            ),
          ),
          hintStyle: AppTypography.inputPlaceholder,
          contentPadding: const EdgeInsets.symmetric(
            horizontal: 16,
            vertical: 14,
          ),
        ),
        // Buttons
        elevatedButtonTheme: ElevatedButtonThemeData(
          style: ElevatedButton.styleFrom(
            backgroundColor: AppColors.accent,
            foregroundColor: AppColors.textInverse,
            elevation: 0,
            shape: RoundedRectangleBorder(
              borderRadius: AppRadius.standardRadius,
            ),
            padding: const EdgeInsets.symmetric(
              horizontal: 16,
              vertical: 12,
            ),
            textStyle: AppTypography.buttonPrimary,
          ),
        ),
        outlinedButtonTheme: OutlinedButtonThemeData(
          style: OutlinedButton.styleFrom(
            foregroundColor: AppColors.textSecondary,
            side: const BorderSide(color: AppColors.border),
            shape: RoundedRectangleBorder(
              borderRadius: AppRadius.standardRadius,
            ),
            padding: const EdgeInsets.symmetric(
              horizontal: 16,
              vertical: 12,
            ),
            textStyle: AppTypography.buttonGhost,
          ),
        ),
        // Cards
        cardTheme: CardThemeData(
          color: AppColors.secondaryBg,
          elevation: 0,
          shape: RoundedRectangleBorder(
            borderRadius: AppRadius.largeRadius,
          ),
          margin: EdgeInsets.zero,
        ),
        // Divider
        dividerTheme: const DividerThemeData(
          color: AppColors.divider,
          thickness: 1,
          space: 0,
        ),
        // Chip
        chipTheme: ChipThemeData(
          backgroundColor: AppColors.pageBg,
          side: const BorderSide(color: AppColors.border),
          shape: RoundedRectangleBorder(
            borderRadius: AppRadius.circleRadius,
          ),
          labelStyle: AppTypography.filterChip,
          padding: const EdgeInsets.symmetric(horizontal: 12, vertical: 6),
        ),
      );
}
```

</action>

<acceptance_criteria>

- `app_theme.dart` contains `abstract final class AppTheme`
- `app_theme.dart` references `AppColors.accent` as primary
- `app_theme.dart` contains `scaffoldBackgroundColor: AppColors.pageBg`
- `app_theme.dart` does NOT contain any dark theme (`ThemeData.dark` or `Brightness.dark`)
- `app_theme.dart` contains `useMaterial3: true` </acceptance_criteria>

---

## Task 5: Create Core Domain Base Classes

<type>code</type>

<read_first>

- (none — standard Clean Architecture base classes) </read_first>

<files>
- mobile/lib/src/core/domain/errors/failures.dart
- mobile/lib/src/core/domain/usecases/usecase.dart
</files>

<action>
Create `mobile/lib/src/core/domain/errors/failures.dart`:

```dart
import 'package:equatable/equatable.dart';

/// Base failure class for domain layer error handling.
abstract class Failure extends Equatable {
  final String message;
  final int? statusCode;

  const Failure({required this.message, this.statusCode});

  @override
  List<Object?> get props => [message, statusCode];
}

class ServerFailure extends Failure {
  const ServerFailure({required super.message, super.statusCode});
}

class CacheFailure extends Failure {
  const CacheFailure({required super.message});
}

class NetworkFailure extends Failure {
  const NetworkFailure({
    super.message = 'No internet connection',
  });
}

class ValidationFailure extends Failure {
  const ValidationFailure({required super.message});
}
```

Create `mobile/lib/src/core/domain/usecases/usecase.dart`:

```dart
import 'package:dartz/dartz.dart';
import 'package:equatable/equatable.dart';

import '../errors/failures.dart';

/// Base UseCase — every use case returns Either<Failure, T>
abstract class UseCase<T, Params> {
  Future<Either<Failure, T>> call(Params params);
}

/// For use cases that take no parameters
class NoParams extends Equatable {
  @override
  List<Object?> get props => [];
}
```

</action>

<acceptance_criteria>

- `failures.dart` contains `abstract class Failure extends Equatable`
- `failures.dart` contains `ServerFailure`, `CacheFailure`, `NetworkFailure`, `ValidationFailure`
- `usecase.dart` contains `abstract class UseCase<T, Params>`
- `usecase.dart` imports `dartz`
- `usecase.dart` contains `class NoParams extends Equatable` </acceptance_criteria>

---

## Task 6: Create Dependency Injection Setup

<type>code</type>

<read_first>

- (none — standard get_it + injectable setup) </read_first>

<files>
- mobile/lib/src/core/di/injection.dart
</files>

<action>
Create `mobile/lib/src/core/di/injection.dart`:

```dart
import 'package:get_it/get_it.dart';

final GetIt sl = GetIt.instance;

/// Initialize all dependency injection bindings.
/// Called once from main.dart before runApp().
Future<void> configureDependencies() async {
  // === Core Services ===
  // (Will be populated as modules are built)

  // === Discovery Module ===
  // (Will be registered in Phase 3 Plan 03)
}
```

</action>

<acceptance_criteria>

- `injection.dart` contains `final GetIt sl = GetIt.instance`
- `injection.dart` contains `Future<void> configureDependencies()` </acceptance_criteria>

---

## Task 7: Create App Router

<type>code</type>

<read_first>

- DESIGN.md (Section 3: Layout & Navigation — drawer, no bottom tab bar)
- .planning/phases/03-discovery-search-screens/03-CONTEXT.md (navigation pattern) </read_first>

<files>
- mobile/lib/src/core/router/app_router.dart
</files>

<action>
Create `mobile/lib/src/core/router/app_router.dart`:

```dart
import 'package:flutter/material.dart';
import 'package:go_router/go_router.dart';

import '../../modules/discovery/presentation/ui/pages/discovery_page.dart';
import '../../modules/discovery/presentation/ui/pages/search_results_page.dart';
import '../../modules/discovery/presentation/ui/pages/near_me_page.dart';
import '../ui/widgets/navigation/lb_app_shell.dart';

/// App-level router configuration.
/// Navigation pattern: Drawer (hamburger) + Avatar dropdown + Contextual FAB.
/// NO bottom tab bar (confirmed per PRD).
final GoRouter appRouter = GoRouter(
  initialLocation: '/',
  routes: [
    ShellRoute(
      builder: (context, state, child) => LbAppShell(child: child),
      routes: [
        GoRoute(
          path: '/',
          name: 'discovery',
          builder: (context, state) => const DiscoveryPage(),
        ),
        GoRoute(
          path: '/search',
          name: 'search',
          builder: (context, state) {
            final query = state.uri.queryParameters['q'] ?? '';
            return SearchResultsPage(query: query);
          },
        ),
        GoRoute(
          path: '/near-me',
          name: 'nearMe',
          builder: (context, state) => const NearMePage(),
        ),
      ],
    ),
  ],
);
```

</action>

<acceptance_criteria>

- `app_router.dart` contains `GoRouter appRouter`
- `app_router.dart` contains route for `/` (discovery)
- `app_router.dart` contains route for `/search`
- `app_router.dart` contains route for `/near-me`
- `app_router.dart` uses `ShellRoute` with `LbAppShell`
- No `BottomNavigationBar` or tab bar references anywhere </acceptance_criteria>

---

## Task 8: Create Shared Widgets (Button, TextField, Card, Badge, Avatar, Loading, Chip, EmptyState)

<type>code</type>

<read_first>

- DESIGN.md (Section 4: Component Specifications — all component specs)
- mobile/lib/src/core/ui/theme/app_colors.dart
- mobile/lib/src/core/ui/theme/app_typography.dart
- mobile/lib/src/core/ui/theme/app_radius.dart </read_first>

<files>
- mobile/lib/src/core/ui/widgets/lb_button.dart
- mobile/lib/src/core/ui/widgets/lb_text_field.dart
- mobile/lib/src/core/ui/widgets/lb_card.dart
- mobile/lib/src/core/ui/widgets/lb_badge.dart
- mobile/lib/src/core/ui/widgets/lb_avatar.dart
- mobile/lib/src/core/ui/widgets/lb_loading.dart
- mobile/lib/src/core/ui/widgets/lb_chip.dart
- mobile/lib/src/core/ui/widgets/lb_empty_state.dart
</files>

<action>
Create the following shared widgets, each matching the DESIGN.md component specifications. Every widget
uses the design tokens from AppColors, AppTypography, AppRadius, AppShadows — NO hardcoded values.

**mobile/lib/src/core/ui/widgets/lb_button.dart**:

- `LbButton` widget with variants: `primary` (Apple Blue bg), `ghost` (outlined), `pill`
  (rounded-980)
- Props: `label`, `onPressed`, `variant` (enum), `isLoading`, `icon`
- Primary: `bg-[#0071e3] text-white rounded-[8px]`
- Ghost: `bg-white border border-slate-200 text-slate-600 rounded-[8px]`
- Pill: `rounded-[980px] border border-[#0071e3] text-[#0071e3]`
- All: minimum touch target 44x44px

**mobile/lib/src/core/ui/widgets/lb_text_field.dart**:

- `LbTextField` widget
- Props: `hint`, `controller`, `onChanged`, `onSubmitted`, `maxLines`, `prefixIcon`, `suffixIcon`
- Styling: `bg-white border border-slate-200 rounded-[8px]`
- Focused: `border-[#0071e3]/40`

**mobile/lib/src/core/ui/widgets/lb_card.dart**:

- `LbCard` widget
- Props: `child`, `elevated` (bool for shadow), `backgroundColor`, `padding`, `onTap`
- Default: `bg-[#f5f5f7] rounded-[12px]` no border
- Elevated: add `shadow-[0_8px_30px_rgba(0,0,0,0.06)]`

**mobile/lib/src/core/ui/widgets/lb_badge.dart**:

- `LbBadge` widget
- Props: `label`, `variant` (neutral, featured)
- Neutral: `bg-slate-100 text-slate-600 text-[11px] rounded-full`
- Featured: `bg-[#0071e3]/10 text-[#0071e3] text-[11px] rounded-full`

**mobile/lib/src/core/ui/widgets/lb_avatar.dart**:

- `LbAvatar` widget
- Props: `imageUrl`, `initials`, `size` (default 32px)
- With image: `rounded-full` with CachedNetworkImage
- Without image: `bg-[#0071e3] rounded-full` with white initial letter

**mobile/lib/src/core/ui/widgets/lb_loading.dart**:

- `LbLoading` widget — centered circular progress indicator in accent blue

**mobile/lib/src/core/ui/widgets/lb_chip.dart**:

- `LbChip` widget
- Props: `label`, `icon`, `isSelected`, `variant` (default, emerald), `onTap`
- Default: `bg-white border border-slate-200 rounded-full text-[13px] font-medium`
- Emerald: `text-emerald-700 bg-emerald-50 border-emerald-200`
- Active press: scale-95 animation

**mobile/lib/src/core/ui/widgets/lb_empty_state.dart**:

- `LbEmptyState` widget
- Props: `message`, `icon`
- Centered, `text-slate-400`, text only (no illustrations for Phase 3) </action>

<acceptance_criteria>

- `lb_button.dart` contains `class LbButton extends StatelessWidget`
- `lb_button.dart` contains `AppColors.accent` for primary variant
- `lb_card.dart` contains `AppColors.secondaryBg`
- `lb_badge.dart` contains `AppColors.accentTintBg` for featured variant
- `lb_avatar.dart` contains `AppColors.accent` for placeholder background
- `lb_chip.dart` contains `AppColors.emeraldText` for emerald variant
- All widgets use `AppColors`, `AppTypography`, `AppRadius` — no hardcoded color/size values
- All interactive widgets have minimum 44x44px touch target </acceptance_criteria>

---

## Task 9: Create Navigation Shell (Drawer + AppBar + AppShell)

<type>code</type>

<read_first>

- DESIGN.md (Section 3: Layout & Navigation — drawer, no bottom tabs)
- DESIGN.md (Section 4: Component Specifications — sidebar nav items)
- .planning/phases/03-discovery-search-screens/03-CONTEXT.md (M-01 through M-04) </read_first>

<files>
- mobile/lib/src/core/ui/widgets/navigation/lb_drawer.dart
- mobile/lib/src/core/ui/widgets/navigation/lb_app_bar.dart
- mobile/lib/src/core/ui/widgets/navigation/lb_app_shell.dart
</files>

<action>
Create `mobile/lib/src/core/ui/widgets/navigation/` directory and files:

**lb_drawer.dart**:

- `LbDrawer` widget — sidebar drawer matching DESIGN.md mobile drawer spec
- Width: 260px, overlay: `bg-black/50`, animation: slide from left 300ms
- Logo badge: 32x32px, rounded-8, `bg-[#0071e3]`, white "LB" text
- Nav items: Discover, Near Me, My Bio, Dashboard (Business only), Bookmarks, Connections, Settings
- Each item: 48px height, px-3, rounded-8
- Active state: `bg-[#0071e3]/10 text-[#0071e3]` + 2px left indicator
- Inactive: `text-slate-600`, hover: `bg-slate-200/50`
- Bottom section: border-top slate-200, login CTA button (Apple Blue full width 40px rounded-8)

**lb_app_bar.dart**:

- `LbAppBar` widget — minimal top bar for mobile
- Height: 48px, frosted glass background (slate-50/90 + blur)
- Leading: hamburger menu icon → opens drawer
- Trailing: avatar icon (32px rounded-full)
- NO search bar in the app bar (search is per-screen)

**lb_app_shell.dart**:

- `LbAppShell` widget — wraps all screens with drawer + app bar
- Props: `child` (Widget)
- Scaffold with `LbAppBar` and `LbDrawer`
- NO `bottomNavigationBar` — confirmed per PRD
- Body: child widget (page content) </action>

<acceptance_criteria>

- `lb_drawer.dart` contains `class LbDrawer extends StatelessWidget`
- `lb_drawer.dart` contains width `260` (sidebar width from DESIGN.md)
- `lb_drawer.dart` contains `AppColors.accent` for active nav item
- `lb_app_bar.dart` contains height `48` (topbar height from DESIGN.md)
- `lb_app_shell.dart` contains `class LbAppShell extends StatelessWidget`
- `lb_app_shell.dart` does NOT contain `bottomNavigationBar`
- `lb_app_shell.dart` uses `Scaffold` with `drawer` property </acceptance_criteria>

---

## Task 10: Update main.dart Entry Point

<type>code</type>

<read_first>

- mobile/lib/main.dart (auto-generated from flutter create)
- mobile/lib/src/core/di/injection.dart
- mobile/lib/src/core/router/app_router.dart
- mobile/lib/src/core/ui/theme/app_theme.dart </read_first>

<files>
- mobile/lib/main.dart
</files>

<action>
Replace `mobile/lib/main.dart` with:

```dart
import 'package:flutter/material.dart';
import 'package:flutter/services.dart';

import 'src/core/di/injection.dart';
import 'src/core/router/app_router.dart';
import 'src/core/ui/theme/app_theme.dart';

void main() async {
  WidgetsFlutterBinding.ensureInitialized();

  // Lock to portrait mode
  await SystemChrome.setPreferredOrientations([
    DeviceOrientation.portraitUp,
    DeviceOrientation.portraitDown,
  ]);

  // Set system UI overlay style (light status bar for light theme)
  SystemChrome.setSystemUIOverlayStyle(
    const SystemUiOverlayStyle(
      statusBarColor: Colors.transparent,
      statusBarIconBrightness: Brightness.dark,
      statusBarBrightness: Brightness.light,
    ),
  );

  // Initialize dependency injection
  await configureDependencies();

  runApp(const LinkBeetApp());
}

class LinkBeetApp extends StatelessWidget {
  const LinkBeetApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp.router(
      title: 'LinkBeet',
      debugShowCheckedModeBanner: false,
      theme: AppTheme.light,
      routerConfig: appRouter,
    );
  }
}
```

</action>

<acceptance_criteria>

- `main.dart` contains `LinkBeetApp`
- `main.dart` contains `configureDependencies()`
- `main.dart` contains `AppTheme.light`
- `main.dart` contains `appRouter`
- `main.dart` contains `MaterialApp.router`
- `main.dart` does NOT contain `ThemeMode.dark` or dark theme references </acceptance_criteria>

</tasks>

<verification>
1. Run `cd mobile && flutter pub get` — exits 0
2. Run `cd mobile && flutter analyze` — no errors (warnings/info acceptable)
3. Verify `mobile/lib/main.dart` uses `AppTheme.light` and `appRouter`
4. Verify `mobile/lib/src/core/ui/theme/app_colors.dart` contains all DESIGN.md color tokens
5. Verify no dark mode references in any theme file
6. Verify no `BottomNavigationBar` in any navigation widget
</verification>

<success_criteria>

- [ ] Full design system created (colors, typography, spacing, radius, shadows, theme)
- [ ] All shared widgets created following DESIGN.md specs
- [ ] Navigation shell (drawer + app bar + app shell) matches DESIGN.md mobile spec
- [ ] Core domain base classes created (UseCase, Failure)
- [ ] DI container initialized with get_it
- [ ] App router configured with go_router (discovery, search, near-me routes)
- [ ] main.dart wired to theme, router, DI
- [ ] No dark mode, no bottom tab bar, no hardcoded values </success_criteria>

<must_haves>

- Design tokens exactly match DESIGN.md values (#0071e3, #ffffff, #f5f5f7, etc.)
- Navigation uses drawer only — NO bottom tab bar
- All widgets use design token constants — NO hardcoded colors/sizes
- AppTheme is light mode ONLY </must_haves>
