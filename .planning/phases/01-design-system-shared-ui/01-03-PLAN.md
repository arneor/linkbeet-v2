---
phase: 1
plan: 3
type: layout
title: 'Navigation Shells & App Layout'
wave: 2
depends_on: [1, 2]
files_modified:
  - web/src/components/layout/Sidebar.tsx
  - web/src/components/layout/TopBar.tsx
  - web/src/components/layout/AppShell.tsx
  - web/src/components/layout/index.ts
  - mobile/src/components/navigation/SidebarDrawer.tsx
  - mobile/src/components/navigation/AvatarDropdown.tsx
  - mobile/src/components/navigation/ContextualFAB.tsx
  - mobile/src/components/navigation/Header.tsx
  - mobile/src/components/navigation/index.ts
autonomous: true
requirements: []
---

<objective>
Build the navigation shells for both web and mobile surfaces following the ChatGPT/Perplexity-style navigation pattern from PRD v2.3. Web gets a persistent collapsible left sidebar (desktop) and minimal top bar (mobile web). Mobile gets a sidebar drawer (hamburger), avatar dropdown, contextual FAB, and header component. NO bottom tab bar.
</objective>

<tasks>

<task id="1" type="create">
<title>Create web Sidebar component</title>
<files>web/src/components/layout/Sidebar.tsx</files>
<read_first>
- DESIGN.md §4 (Component Stylings — Navigation)
- .planning/PROJECT.md §Navigation Pattern
- web/src/app/layout.tsx
</read_first>
<action>
Create `web/src/components/layout/Sidebar.tsx` — the persistent collapsible left sidebar for desktop web (ChatGPT-style):

```tsx
'use client'
```

Props: `SidebarProps` with:

- `isCollapsed: boolean`
- `onToggle: () => void`
- `currentPath: string`
- `userMode?: 'normal' | 'business'`
- `isLoggedIn?: boolean`

Structure:

1. **Fixed left sidebar**: `w-[260px]` expanded, `w-[72px]` collapsed. Height `100vh`. Position
   fixed left.
2. **Navigation glass effect**: bg `rgba(0,0,0,0.8)` with
   `backdrop-filter: saturate(180%) blur(20px)` — per DESIGN.md §4 Navigation.
3. **Logo area**: Top section with LinkBeet logo/wordmark. In collapsed state, show only icon.
4. **Nav items**:
   - Discover
   - Near Me
   - My Bio
   - Dashboard (shown ONLY when `userMode === 'business'`)
   - Bookmarks
   - Connections
   - Settings
5. **Nav item styling**:
   - Text: 12px white, weight 400 (per DESIGN.md navigation)
   - Active: left border 2px `#0071e3` (Apple Blue), text `#2997ff`
   - Hover: bg `rgba(255,255,255,0.1)`
   - Icon + label layout (icon-only when collapsed)
   - 48px row height
6. **Bottom section**: Login/Signup button (if not logged in), or user avatar + name (if logged in)
7. **Collapse toggle**: Bottom of sidebar, chevron icon, rotates on toggle
8. **Smooth transition**: `transition-all duration-300 ease-in-out` on width change

Accessibility: `nav` landmark, `aria-label="Main navigation"`, `role="navigation"`. </action>
<acceptance_criteria>

- `web/src/components/layout/Sidebar.tsx` exists with `'use client'` directive
- Sidebar has `isCollapsed` prop controlling width (260px vs 72px)
- Sidebar has nav-glass styling: `backdrop-filter: saturate(180%) blur(20px)` on dark bg
- Sidebar renders 7 menu items: Discover, Near Me, My Bio, Dashboard, Bookmarks, Connections,
  Settings
- Dashboard item is conditionally rendered based on `userMode === 'business'`
- Active nav item has Apple Blue accent (`#0071e3` or `#2997ff`)
- Sidebar has `nav` element with `aria-label`
- Text uses 12px size per DESIGN.md navigation spec </acceptance_criteria> </task>

<task id="2" type="create">
<title>Create web TopBar component (mobile web)</title>
<files>web/src/components/layout/TopBar.tsx</files>
<read_first>
- DESIGN.md §4 (Component Stylings — Navigation)
- .planning/PROJECT.md §Navigation Pattern — Web
</read_first>
<action>
Create `web/src/components/layout/TopBar.tsx` — minimal top bar for mobile web view:

```tsx
'use client'
```

Props: `TopBarProps` with:

- `onMenuToggle: () => void` — opens sidebar drawer
- `userAvatar?: string`
- `userName?: string`
- `isLoggedIn?: boolean`

Structure:

1. **Fixed top bar**: Height 48px (per DESIGN.md nav height). Full width. z-50.
2. **Navigation glass**: Same `rgba(0,0,0,0.8)` + blur effect as sidebar.
3. **Left**: Hamburger menu icon (3 horizontal lines) — triggers `onMenuToggle`
4. **Center**: LinkBeet wordmark/logo
5. **Right**: Avatar (if logged in) or "Login" text link (if not)

Styling:

- All text: 12px white weight 400
- Icons: 20px, white
- Hamburger: hover opacity change
- Avatar: 28px circle
- Bottom border: subtle `rgba(255,255,255,0.1)` 1px

Only visible below tablet breakpoint (`block md:hidden` or similar). On desktop, sidebar handles
navigation. </action> <acceptance_criteria>

- `web/src/components/layout/TopBar.tsx` exists with `'use client'` directive
- TopBar height is 48px (DESIGN.md nav height)
- TopBar uses nav-glass styling (translucent dark + blur)
- TopBar has hamburger menu on left, logo center, avatar/login right
- TopBar is only visible on mobile web (hidden on desktop)
- Text uses 12px white per DESIGN.md navigation </acceptance_criteria> </task>

<task id="3" type="create">
<title>Create web AppShell layout component</title>
<files>web/src/components/layout/AppShell.tsx</files>
<read_first>
- web/src/components/layout/Sidebar.tsx
- web/src/components/layout/TopBar.tsx
- web/src/app/layout.tsx
</read_first>
<action>
Create `web/src/components/layout/AppShell.tsx` — the main layout shell that combines Sidebar + TopBar:

```tsx
'use client'
```

Props: `AppShellProps` with:

- `children: React.ReactNode`
- `currentPath: string`
- `userMode?: 'normal' | 'business'`
- `isLoggedIn?: boolean`
- `userAvatar?: string`
- `userName?: string`

Behavior:

1. Manages sidebar collapsed state via `useState` (persisted to localStorage key
   `linkbeet-sidebar-collapsed`)
2. Manages mobile drawer open state
3. Desktop (>= 1024px): Show Sidebar (fixed left) + main content area with left margin matching
   sidebar width
4. Mobile (<1024px): Show TopBar (fixed top) + main content with top padding for bar height +
   sidebar as overlay drawer (slides in from left with backdrop)
5. Main content area: max-width 980px centered (per DESIGN.md)

Use `useMediaQuery` or CSS-based responsive approach. </action> <acceptance_criteria>

- `web/src/components/layout/AppShell.tsx` exists with `'use client'` directive
- AppShell renders Sidebar on desktop, TopBar on mobile
- AppShell manages sidebar collapsed state in localStorage
- Main content has max-width 980px centered
- Mobile drawer has backdrop overlay
- Sidebar width transition is smooth (duration-300) </acceptance_criteria> </task>

<task id="4" type="create">
<title>Create web layout barrel export</title>
<files>web/src/components/layout/index.ts</files>
<read_first>
- web/src/components/layout/Sidebar.tsx
- web/src/components/layout/TopBar.tsx
- web/src/components/layout/AppShell.tsx
</read_first>
<action>
Create `web/src/components/layout/index.ts`:

```typescript
export { Sidebar } from './Sidebar'
export type { SidebarProps } from './Sidebar'

export { TopBar } from './TopBar'
export type { TopBarProps } from './TopBar'

export { AppShell } from './AppShell'
export type { AppShellProps } from './AppShell'
```

</action>
<acceptance_criteria>
- `web/src/components/layout/index.ts` exports `Sidebar`, `TopBar`, `AppShell`
- All types are exported alongside components
</acceptance_criteria>
</task>

<task id="5" type="create">
<title>Create mobile SidebarDrawer component</title>
<files>mobile/src/components/navigation/SidebarDrawer.tsx</files>
<read_first>
- .planning/PROJECT.md §Navigation Pattern — Mobile
- DESIGN.md §4 (Component Stylings — Navigation)
- mobile/src/theme/tokens.ts
</read_first>
<action>
Create `mobile/src/components/navigation/SidebarDrawer.tsx` — the hamburger-triggered drawer:

Props:

- `isOpen: boolean`
- `onClose: () => void`
- `currentRoute: string`
- `userMode?: 'normal' | 'business'`
- `isLoggedIn?: boolean`
- `userName?: string`
- `userAvatar?: string`

Structure (React Native):

1. **Overlay**: `Pressable` full screen, bg `rgba(0,0,0,0.5)`, closes drawer on press
2. **Drawer**: Slides in from left, width `80%` max 300px, full height, bg `#000000` (pure black per
   DESIGN.md dark section)
3. **User section** (top): Avatar (40px) + name + mode badge (Normal/Business)
4. **Nav items**: Same 7 items as web sidebar:
   - Discover, Near Me, My Bio, Dashboard (business only), Bookmarks, Connections, Settings
5. **Nav item styling**:
   - Text: white, 17px (body size for mobile readability)
   - Active: left border 3px `#0071e3`, text `#2997ff`
   - Row height: 52px (comfortable touch target >44px)
   - Icon left + label right
6. **Bottom**: Login/Signup (if anonymous), Logout (if logged in)
7. **Animation**: `Animated.View` with `translateX` slide, 300ms duration

Import colors, spacing from `mobile/src/theme/tokens`. </action> <acceptance_criteria>

- `mobile/src/components/navigation/SidebarDrawer.tsx` exists
- Drawer slides from left with animation
- Drawer renders 7 nav items with Dashboard conditional on business mode
- Active item highlighted with Apple Blue (#0071e3)
- Drawer has overlay that closes on press
- Nav items have minimum 44px touch target
- Drawer bg is #000000 (DESIGN.md dark) </acceptance_criteria> </task>

<task id="6" type="create">
<title>Create mobile AvatarDropdown component</title>
<files>mobile/src/components/navigation/AvatarDropdown.tsx</files>
<read_first>
- .planning/PROJECT.md §Navigation Pattern — Mobile (Avatar dropdown top right)
- mobile/src/theme/tokens.ts
</read_first>
<action>
Create `mobile/src/components/navigation/AvatarDropdown.tsx`:

Props:

- `isOpen: boolean`
- `onToggle: () => void`
- `userName?: string`
- `userAvatar?: string`
- `userMode?: 'normal' | 'business'`
- `isLoggedIn?: boolean`

Structure:

1. **Trigger**: Avatar circle (32px) in top-right header area — `Pressable` that toggles dropdown
2. **Dropdown panel**: Positioned below avatar, bg `#272729` (dark surface 1), rounded 12px, shadow
   card, min-width 200px
3. **Contents** (when logged in):
   - User name + email/phone
   - Mode badge (Normal/Business)
   - Quick links: Edit Profile, Settings, Switch to Business (if normal mode)
   - Logout
4. **Contents** (when anonymous):
   - "Login / Sign Up" button
5. **Dismiss**: Taps outside close dropdown

Use `Animated.View` with scale + opacity animation for smooth appear/disappear. </action>
<acceptance_criteria>

- `mobile/src/components/navigation/AvatarDropdown.tsx` exists
- Dropdown has 32px circular avatar trigger
- Dropdown panel bg is `#272729` (dark surface) with rounded 12px
- Shows mode badge (Normal/Business)
- Shows "Switch to Business" when userMode is 'normal'
- Dropdown dismisses on outside tap
- Has Login/Sign Up for anonymous users </acceptance_criteria> </task>

<task id="7" type="create">
<title>Create mobile ContextualFAB component</title>
<files>mobile/src/components/navigation/ContextualFAB.tsx</files>
<read_first>
- .planning/PROJECT.md §Navigation Pattern — Mobile (Contextual FAB bottom right)
- DESIGN.md §4 (Button — Primary Blue CTA)
- mobile/src/theme/tokens.ts
</read_first>
<action>
Create `mobile/src/components/navigation/ContextualFAB.tsx`:

Props:

- `icon: React.ReactNode` — Lucide icon or similar
- `label?: string` — optional tooltip text
- `onPress: () => void`
- `variant?: 'primary' | 'secondary'`
- `visible?: boolean` — controls show/hide with animation

Structure:

1. **Position**: Absolute bottom-right, 16px margin from edges
2. **Primary variant**: 56px circle, bg `#0071e3` (Apple Blue), white icon, shadow card
3. **Secondary variant**: 56px circle, bg `#1d1d1f`, white icon
4. **Press animation**: Scale to 0.9 on press (`Pressable` with `Animated.View`)
5. **Show/hide**: Fade + scale animation when `visible` changes
6. **Accessibility**: `accessibilityLabel` with `label` prop, `accessibilityRole="button"`

The FAB is context-aware (different screens show different icons/actions):

- Discovery: search icon
- Dashboard: "+" add listing
- Profile: edit icon
- This component is the shell — screen-specific configuration is passed via props. </action>
  <acceptance_criteria>
- `mobile/src/components/navigation/ContextualFAB.tsx` exists
- FAB is 56px circle positioned bottom-right
- Primary variant uses bg `#0071e3` (Apple Blue)
- FAB has press animation (scale 0.9)
- FAB has show/hide animation (fade + scale)
- FAB has `accessibilityRole="button"` and `accessibilityLabel` </acceptance_criteria> </task>

<task id="8" type="create">
<title>Create mobile Header component</title>
<files>mobile/src/components/navigation/Header.tsx</files>
<read_first>
- DESIGN.md §4 (Component Stylings — Navigation)
- mobile/src/components/navigation/AvatarDropdown.tsx
- mobile/src/theme/tokens.ts
</read_first>
<action>
Create `mobile/src/components/navigation/Header.tsx`:

Props:

- `title?: string`
- `showBack?: boolean` — show back arrow instead of hamburger
- `onBack?: () => void`
- `onMenuPress: () => void` — opens sidebar drawer
- `rightContent?: React.ReactNode` — typically AvatarDropdown
- `transparent?: boolean` — for hero screens with bg image

Structure:

1. **Height**: 48px (per DESIGN.md nav height) + SafeAreaView top inset
2. **Left**: Hamburger icon (default) OR back arrow (when `showBack`)
3. **Center**: Title text (17px, weight 600, white)
4. **Right**: `rightContent` slot (usually AvatarDropdown)
5. **Background**:
   - Default: `rgba(0,0,0,0.8)` with blur (nav-glass)
   - Transparent: fully transparent (for hero/discovery screens)
6. **All touch targets**: minimum 44x44px

Import from theme tokens for colors and spacing. </action> <acceptance_criteria>

- `mobile/src/components/navigation/Header.tsx` exists
- Header height is 48px + SafeAreaView inset
- Header has hamburger/back toggle via `showBack` prop
- Header supports transparent mode for hero screens
- Header default bg uses nav-glass pattern (translucent dark + blur)
- Title text is 17px weight 600
- All touch targets are minimum 44px </acceptance_criteria> </task>

<task id="9" type="create">
<title>Create mobile navigation barrel export</title>
<files>mobile/src/components/navigation/index.ts</files>
<read_first>
- mobile/src/components/navigation/SidebarDrawer.tsx
- mobile/src/components/navigation/AvatarDropdown.tsx
- mobile/src/components/navigation/ContextualFAB.tsx
- mobile/src/components/navigation/Header.tsx
</read_first>
<action>
Create `mobile/src/components/navigation/index.ts`:

```typescript
export { SidebarDrawer } from './SidebarDrawer'
export { AvatarDropdown } from './AvatarDropdown'
export { ContextualFAB } from './ContextualFAB'
export { Header } from './Header'
```

</action>
<acceptance_criteria>
- `mobile/src/components/navigation/index.ts` exports all 4 navigation components
</acceptance_criteria>
</task>

</tasks>

<verification>
1. Verify web Sidebar renders all 7 nav items with correct conditional logic
2. Verify web TopBar only shows on mobile breakpoint
3. Verify AppShell manages sidebar state and responsive layout
4. Verify mobile SidebarDrawer has slide animation and correct items
5. Verify mobile Header respects 48px height + safe area
6. Verify all navigation components use Apple design system colors/styling
7. Run TypeScript check on both web and mobile navigation components
</verification>

<success_criteria>

- Web: Sidebar (collapsible, glass effect) + TopBar (mobile web) + AppShell (orchestrator)
- Mobile: SidebarDrawer + AvatarDropdown + ContextualFAB + Header
- NO bottom tab bar on any surface
- All nav uses translucent glass effect per DESIGN.md
- Dashboard item conditional on Business mode
- All touch targets >= 44px
- Smooth animations on all interactive navigation elements </success_criteria>

<must_haves>

- [ ] No bottom tab bar
- [ ] Navigation glass effect on all nav components
- [ ] Dashboard conditional on Business mode
- [ ] 48px nav height per DESIGN.md
- [ ] Sidebar items match PRD: Discover, Near Me, My Bio, Dashboard, Bookmarks, Connections,
      Settings
- [ ] Mobile FAB with Apple Blue (#0071e3) </must_haves>
