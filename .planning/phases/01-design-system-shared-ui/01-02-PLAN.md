---
phase: 1
plan: 2
type: component
title: 'Shared UI Components'
wave: 1
depends_on: []
files_modified:
  - shared/ui/src/components/Button.tsx
  - shared/ui/src/components/Card.tsx
  - shared/ui/src/components/Input.tsx
  - shared/ui/src/components/Avatar.tsx
  - shared/ui/src/components/Badge.tsx
  - shared/ui/src/components/Modal.tsx
  - shared/ui/src/components/LoadingState.tsx
  - shared/ui/src/components/EmptyState.tsx
  - shared/ui/src/index.ts
autonomous: true
requirements: []
---

<objective>
Rewrite existing Button and Card components and create new shared UI primitives (Input, Avatar, Badge, Modal, LoadingState, EmptyState) following the Apple-inspired design system from DESIGN.md. All components use Tailwind CSS classes matching the design tokens established in Plan 01. Components are platform-agnostic React components consumed by web and admin apps.
</objective>

<tasks>

<task id="1" type="modify">
<title>Rewrite Button component with Apple variants</title>
<files>shared/ui/src/components/Button.tsx</files>
<read_first>
- shared/ui/src/components/Button.tsx
- DESIGN.md §4 (Component Stylings — Buttons)
</read_first>
<action>
Replace `shared/ui/src/components/Button.tsx` with Apple-inspired button variants. Button types from DESIGN.md:

1. **primaryBlue** — bg `#0071e3`, text white, radius 8px, padding 8px 15px, font 17px/400
2. **primaryDark** — bg `#1d1d1f`, text white, radius 8px, padding 8px 15px
3. **pillLink** — transparent bg, text `#0066cc` (light) or `#2997ff` (dark), radius 980px, border
   1px solid, hover underline
4. **filter** — bg `#fafafc`, text `rgba(0,0,0,0.8)`, radius 11px, border 3px solid
   `rgba(0,0,0,0.04)`, padding 0 14px
5. **mediaControl** — bg `rgba(210,210,215,0.64)`, radius 50%, active scale(0.9)
6. **ghost** — transparent bg, text inherits, hover bg subtle

Sizes: `sm` (px-3 py-1.5 text-sm), `md` (px-4 py-2 text-[17px]), `lg` (px-6 py-3 text-lg)

All buttons:

- Focus: `outline: 2px solid #0071e3` (Apple Blue focus ring)
- Disabled: opacity 50%, cursor not-allowed
- Transition: `transition-colors duration-150`
- Loading spinner: `h-4 w-4 animate-spin rounded-full border-2 border-current border-t-transparent`

Keep existing props interface (`ButtonVariant`, `ButtonSize`, `ButtonProps` with `isLoading`,
`leftIcon`, `rightIcon`). Update `ButtonVariant` type to
`'primaryBlue' | 'primaryDark' | 'pillLink' | 'filter' | 'mediaControl' | 'ghost'`.

Use `cn()` from `../lib/utils` for class merging. Keep `React.forwardRef` pattern.

Variant classes:

```
primaryBlue: 'bg-[#0071e3] text-white hover:bg-[#0077ED] active:bg-[#0068D1] rounded-[8px] px-[15px] py-[8px]'
primaryDark: 'bg-[#1d1d1f] text-white hover:bg-[#2d2d2f] active:bg-[#0d0d0f] rounded-[8px] px-[15px] py-[8px]'
pillLink: 'bg-transparent text-[#0066cc] border border-[#0066cc] rounded-[980px] px-[15px] py-[8px] hover:underline'
filter: 'bg-[#fafafc] text-[rgba(0,0,0,0.8)] border-[3px] border-[rgba(0,0,0,0.04)] rounded-[11px] px-[14px] py-0'
mediaControl: 'bg-[rgba(210,210,215,0.64)] text-[rgba(0,0,0,0.48)] rounded-full active:scale-90'
ghost: 'bg-transparent hover:bg-black/5 active:bg-black/10'
```

Focus for all:
`'focus-visible:outline-2 focus-visible:outline-[#0071e3] focus-visible:outline-offset-2'` </action>
<acceptance_criteria>

- `Button.tsx` exports `ButtonVariant` type with values
  `'primaryBlue' | 'primaryDark' | 'pillLink' | 'filter' | 'mediaControl' | 'ghost'`
- `Button.tsx` has variant class `bg-[#0071e3]` for `primaryBlue`
- `Button.tsx` has variant class `rounded-[980px]` for `pillLink`
- `Button.tsx` has focus class containing `outline-[#0071e3]`
- `Button.tsx` maintains `isLoading`, `leftIcon`, `rightIcon` props
- No violet/slate color classes remain </acceptance_criteria> </task>

<task id="2" type="modify">
<title>Rewrite Card component with Apple variants</title>
<files>shared/ui/src/components/Card.tsx</files>
<read_first>
- shared/ui/src/components/Card.tsx
- DESIGN.md §4 (Component Stylings — Cards & Containers)
</read_first>
<action>
Update `shared/ui/src/components/Card.tsx` to have Apple-inspired variants:

Card variants from DESIGN.md:

1. **light** — bg `#f5f5f7`, no border, radius 8px, no shadow
2. **dark** — bg `#272729` to `#2a2a2d`, no border, radius 8px, no shadow
3. **elevated** — bg white, shadow `rgba(0,0,0,0.22) 3px 5px 30px 0px`, radius 8px
4. **ghost** — bg transparent, no border, no shadow

Update `CardProps.variant` type to `'light' | 'dark' | 'elevated' | 'ghost'`, default `'light'`.

Variant classes:

```
light: 'bg-[#f5f5f7] rounded-[8px]'
dark: 'bg-[#272729] text-white rounded-[8px]'
elevated: 'bg-white rounded-[8px] shadow-[3px_5px_30px_rgba(0,0,0,0.22)]'
ghost: 'bg-transparent'
```

All cards: generous padding (`p-6`), centered content. NO borders (Apple almost never uses borders
on cards). Keep CardHeader, CardTitle, CardContent, CardFooter sub-components but update their
styling:

- CardTitle: `font-semibold text-[21px] leading-[1.19] tracking-[0.231px]` (matches DESIGN.md
  cardTitle)
- CardHeader: `flex flex-col space-y-1.5 pb-4`
- CardContent: no extra styling
- CardFooter: `flex items-center pt-4`

Remove any `border`, `border-slate-200` references. No shadow on non-elevated variants. </action>
<acceptance_criteria>

- `Card.tsx` exports `CardProps` with `variant` type `'light' | 'dark' | 'elevated' | 'ghost'`
- `Card.tsx` has class `bg-[#f5f5f7]` for `light` variant
- `Card.tsx` has class `bg-[#272729]` for `dark` variant
- `Card.tsx` has class `shadow-[3px_5px_30px_rgba(0,0,0,0.22)]` for `elevated` variant
- No `border-slate-200` or `border` classes on card variants
- No `shadow-sm` (old generic shadow) remains
- CardTitle uses `text-[21px]` and `leading-[1.19]` </acceptance_criteria> </task>

<task id="3" type="create">
<title>Create Input component</title>
<files>shared/ui/src/components/Input.tsx</files>
<read_first>
- DESIGN.md §4 (Component Stylings — Filter/Search Button for input styling cues)
- shared/ui/src/components/Button.tsx (for pattern reference)
</read_first>
<action>
Create `shared/ui/src/components/Input.tsx` — a text input component following Apple styling cues from DESIGN.md:

Props: `InputProps extends React.InputHTMLAttributes<HTMLInputElement>` with:

- `label?: string` — optional floating/static label
- `error?: string` — error message
- `leftIcon?: React.ReactNode`
- `rightIcon?: React.ReactNode`
- `variant?: 'default' | 'search'`

Styling:

- **default**: bg `#fafafc`, rounded `11px` (comfortable radius), border
  `1px solid rgba(0,0,0,0.04)`, font 17px, padding `8px 14px`, text `#1d1d1f`
- **search**: same as default but with search icon placeholder, rounded `11px`
- Focus: `outline: 2px solid #0071e3` (Apple Blue focus ring), `outline-offset: 2px`
- Error state: border `#EF4444`, error text 14px `#EF4444` below input
- Disabled: opacity 50%

Use React.forwardRef, cn() utility. Export `InputProps` type. </action> <acceptance_criteria>

- `shared/ui/src/components/Input.tsx` exists
- File exports `Input` component and `InputProps` type
- Input has `variant` prop with `'default' | 'search'` options
- Input has focus class with `outline-[#0071e3]`
- Input has `rounded-[11px]` (comfortable radius from DESIGN.md)
- Input supports `label`, `error`, `leftIcon`, `rightIcon` props </acceptance_criteria> </task>

<task id="4" type="create">
<title>Create Avatar component</title>
<files>shared/ui/src/components/Avatar.tsx</files>
<read_first>
- DESIGN.md §5 (Layout Principles — Border Radius Scale)
</read_first>
<action>
Create `shared/ui/src/components/Avatar.tsx`:

Props: `AvatarProps` with:

- `src?: string` — image URL
- `alt?: string` — alt text
- `name?: string` — for initials fallback
- `size?: 'sm' | 'md' | 'lg' | 'xl'` — sm=32px, md=40px, lg=56px, xl=80px
- `className?: string`

Behavior:

- If `src` provided, render `<img>` with `rounded-full`, `object-cover`
- If no `src` but `name` provided, render initials (first letter or first+last) on Apple Blue
  `#0071e3` bg with white text
- If neither, render a generic user icon placeholder
- All sizes: `rounded-full` (circle)

Use React.forwardRef, cn() utility. </action> <acceptance_criteria>

- `shared/ui/src/components/Avatar.tsx` exists
- File exports `Avatar` and `AvatarProps`
- Avatar shows initials when no `src` — bg `#0071e3` text white
- Avatar has 4 sizes: sm=32px, md=40px, lg=56px, xl=80px
- Avatar uses `rounded-full` for circle shape </acceptance_criteria> </task>

<task id="5" type="create">
<title>Create Badge component</title>
<files>shared/ui/src/components/Badge.tsx</files>
<read_first>
- DESIGN.md §2 (Color Palette — Interactive), §5 (Border Radius Scale)
</read_first>
<action>
Create `shared/ui/src/components/Badge.tsx`:

Props: `BadgeProps` with:

- `variant?: 'default' | 'interactive' | 'success' | 'warning' | 'error'`
- `size?: 'sm' | 'md'`
- `children: React.ReactNode`
- `className?: string`

Styling:

- **default**: bg `#f5f5f7`, text `#1d1d1f`, rounded `5px` (micro radius)
- **interactive**: bg `#0071e3`, text white, rounded `5px`
- **success**: bg `#D1FAE5`, text `#065F46`, rounded `5px`
- **warning**: bg `#FEF3C7`, text `#92400E`, rounded `5px`
- **error**: bg `#FEE2E2`, text `#991B1B`, rounded `5px`

Sizes: sm = `text-[10px] px-1.5 py-0.5`, md = `text-[12px] px-2 py-0.5`

Use `cn()`. Font weight 600 (semibold). Inline element (`inline-flex items-center`). </action>
<acceptance_criteria>

- `shared/ui/src/components/Badge.tsx` exists
- File exports `Badge` and `BadgeProps`
- Badge `interactive` variant uses bg `#0071e3` (Apple Blue)
- Badge `default` variant uses bg `#f5f5f7` (light gray)
- Badge uses `rounded-[5px]` (micro radius)
- Badge has 2 sizes and 5 variants </acceptance_criteria> </task>

<task id="6" type="create">
<title>Create Modal/Dialog component</title>
<files>shared/ui/src/components/Modal.tsx</files>
<read_first>
- DESIGN.md §6 (Depth & Elevation)
</read_first>
<action>
Create `shared/ui/src/components/Modal.tsx` — a dialog/modal component:

Props: `ModalProps` with:

- `isOpen: boolean`
- `onClose: () => void`
- `title?: string`
- `children: React.ReactNode`
- `size?: 'sm' | 'md' | 'lg'` — sm=400px, md=560px, lg=720px max-width
- `className?: string`

Structure:

- Overlay: `fixed inset-0 bg-black/50 z-50` with fade animation
- Content: centered, bg white, rounded `12px` (large radius), shadow card, max-height `85vh`,
  overflow auto
- Close button: top-right, circular (50% radius), hover bg `rgba(255,255,255,0.32)` on dark
  backgrounds
- Title: 21px weight 600 (cardTitle style from DESIGN.md)
- Smooth open/close transitions: opacity + scale transform

Accessibility: trap focus, close on Escape, close on overlay click, aria-modal="true". </action>
<acceptance_criteria>

- `shared/ui/src/components/Modal.tsx` exists
- File exports `Modal` and `ModalProps`
- Modal has overlay with `bg-black/50`
- Modal content uses `rounded-[12px]` (large radius)
- Modal has 3 sizes: sm=400px, md=560px, lg=720px
- Modal closes on Escape key press
- Modal closes on overlay click
- Modal traps focus (aria-modal="true") </acceptance_criteria> </task>

<task id="7" type="create">
<title>Create LoadingState component</title>
<files>shared/ui/src/components/LoadingState.tsx</files>
<read_first>
- DESIGN.md §2 (Color Palette)
</read_first>
<action>
Create `shared/ui/src/components/LoadingState.tsx`:

Props: `LoadingStateProps` with:

- `variant?: 'spinner' | 'skeleton' | 'dots'`
- `size?: 'sm' | 'md' | 'lg'`
- `label?: string` — optional "Loading..." text
- `className?: string`

Variants:

- **spinner**: Circular spinner using Apple Blue `#0071e3` border animation, `animate-spin`. Sizes:
  sm=16px, md=24px, lg=40px.
- **skeleton**: Pulsing rectangles with `bg-[#f5f5f7]` animated shimmer (pulse). Use
  `animate-pulse`. Render 3 skeleton bars of varying width.
- **dots**: Three dots with staggered bounce animation using Apple Blue.

Centered in container, with optional label text below in caption style (14px, `rgba(0,0,0,0.48)`).
</action> <acceptance_criteria>

- `shared/ui/src/components/LoadingState.tsx` exists
- File exports `LoadingState` and `LoadingStateProps`
- LoadingState `spinner` variant uses border color `#0071e3`
- LoadingState `skeleton` uses bg `#f5f5f7` with `animate-pulse`
- LoadingState has 3 variants and 3 sizes
- Optional `label` prop renders below the loader </acceptance_criteria> </task>

<task id="8" type="create">
<title>Create EmptyState component</title>
<files>shared/ui/src/components/EmptyState.tsx</files>
<read_first>
- DESIGN.md §3 (Typography — Section Heading, Body)
</read_first>
<action>
Create `shared/ui/src/components/EmptyState.tsx`:

Props: `EmptyStateProps` with:

- `icon?: React.ReactNode` — optional icon (from Lucide)
- `title: string`
- `description?: string`
- `action?: React.ReactNode` — optional CTA button
- `className?: string`

Layout: Centered flexbox column. Generous padding (48px vertical).

Styling:

- Icon: 48px, color `rgba(0,0,0,0.48)` (tertiary text), margin-bottom 16px
- Title: 21px weight 600 (subHeading from DESIGN.md), color `#1d1d1f`, margin-bottom 8px
- Description: 17px weight 400 (body), color `rgba(0,0,0,0.48)`, max-width 400px, text-center
- Action: margin-top 24px (render whatever ReactNode is passed — typically a Button) </action>
  <acceptance_criteria>
- `shared/ui/src/components/EmptyState.tsx` exists
- File exports `EmptyState` and `EmptyStateProps`
- EmptyState has `icon`, `title`, `description`, `action` props
- Title uses `text-[21px] font-semibold` styling
- Description uses `text-[17px]` with tertiary color
- Component is centered with `flex flex-col items-center` </acceptance_criteria> </task>

<task id="9" type="modify">
<title>Update shared UI barrel exports</title>
<files>shared/ui/src/index.ts</files>
<read_first>
- shared/ui/src/index.ts
</read_first>
<action>
Update `shared/ui/src/index.ts` to export all new and updated components:

```typescript
export { Button } from './components/Button'
export type { ButtonProps, ButtonVariant, ButtonSize } from './components/Button'

export { Card, CardHeader, CardTitle, CardContent, CardFooter } from './components/Card'
export type {
  CardProps,
  CardHeaderProps,
  CardTitleProps,
  CardContentProps,
  CardFooterProps,
} from './components/Card'

export { Input } from './components/Input'
export type { InputProps } from './components/Input'

export { Avatar } from './components/Avatar'
export type { AvatarProps } from './components/Avatar'

export { Badge } from './components/Badge'
export type { BadgeProps } from './components/Badge'

export { Modal } from './components/Modal'
export type { ModalProps } from './components/Modal'

export { LoadingState } from './components/LoadingState'
export type { LoadingStateProps } from './components/LoadingState'

export { EmptyState } from './components/EmptyState'
export type { EmptyStateProps } from './components/EmptyState'

export { cn } from './lib/utils'
```

</action>
<acceptance_criteria>
- `shared/ui/src/index.ts` exports `Button`, `Card`, `Input`, `Avatar`, `Badge`, `Modal`, `LoadingState`, `EmptyState`
- `shared/ui/src/index.ts` exports all associated Props types
- `shared/ui/src/index.ts` exports `cn` utility
- File has 8 component exports total (Button, Card, Input, Avatar, Badge, Modal, LoadingState, EmptyState)
</acceptance_criteria>
</task>

</tasks>

<verification>
1. Run `npx tsc --noEmit` in shared/ui — all components type-check
2. Verify Button has 6 variants matching DESIGN.md (primaryBlue, primaryDark, pillLink, filter, mediaControl, ghost)
3. Verify Card has no borders per DESIGN.md
4. Verify all new components use Apple-system colors (#0071e3, #f5f5f7, #1d1d1f)
5. Verify index.ts exports all 8 components and their types
</verification>

<success_criteria>

- Button: 6 variants matching DESIGN.md exactly
- Card: 4 variants (light, dark, elevated, ghost) — NO borders
- Input: Apple filter/search style with comfortable radius
- Avatar: Circular, 4 sizes, initials fallback with Apple Blue
- Badge: 5 variants, micro radius
- Modal: Accessible, 3 sizes, Apple depth styling
- LoadingState: 3 variants (spinner, skeleton, dots)
- EmptyState: Centered, with icon/title/description/action
- All components use cn() and React.forwardRef patterns
- No violet, indigo, or slate brand colors remain </success_criteria>

<must_haves>

- [ ] Button pillLink variant with 980px radius
- [ ] Card has NO borders
- [ ] All focus rings use Apple Blue #0071e3
- [ ] All components exported from index.ts
- [ ] Modal accessible (focus trap, Escape close, aria-modal) </must_haves>
